# Application Security Verification Standard crosswalk

This document crosswalks this base template against the [OWASP Application Security Verification Standard (ASVS) 5.0.0](https://github.com/OWASP/ASVS/tree/v5.0.0/5.0). It is a planning and verification aid, not an ASVS conformance claim.

The table contains all 345 ASVS 5.0.0 requirements. Requirement text is reproduced from OWASP ASVS 5.0.0, which is licensed under [CC BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/). Use version-qualified identifiers such as `v5.0.0-1.1.1` in evidence, tickets, and test cases so future ASVS versions cannot silently change the control being referenced.

## Scope and status meanings

The crosswalk covers the application and the security capabilities intentionally supplied by this template. It does not certify the application that an adopter builds from it, its identity provider, data stores, deployment platform, networks, monitoring stack, or operational procedures. Each template adopter must reassess the rows when adding features or choosing a deployment topology.

| Status | Meaning |
| --- | --- |
| Configured | The template explicitly implements the requirement's stated control. Deployment-specific verification is still required. |
| Verify framework behavior | The control is supplied by Spring Security or another dependency rather than a template-owned setting. Keep an integration test and recheck after upgrades. |
| Verification required | The requirement may be relevant, but the base template has no sufficient product-specific evidence. The adopter must design, implement, and test it. |
| Deployment decision required | The control belongs to the configured identity provider, infrastructure, secret manager, or operational process. The template cannot choose the value safely. |
| Not applicable to this template | The template does not provide the relevant capability or act in the ASVS role. Reassess before introducing that capability or role. |

For the template's related evidence, see [security authentication](authentication.md), [security headers](headers.md), [security logging](logging/README.md), [security logging schema](logging/schema.md), and [Tomcat hardening](hardening.md).

## V1: Encoding and Sanitization

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V1.1.1 | 2 | Verify that input is decoded or unescaped into a canonical form only once, it is only decoded when encoded data in that form is expected, and that this is done before processing the input further, for example it is not performed after input validation or sanitization. | Verify framework behavior | Query-string and path decoding is performed once by the servlet container and Spring MVC, following the platform's URL-decoding rules; the application performs no custom or repeated decoding of its own. |
| V1.1.2 | 2 | Verify that the application performs output encoding and escaping either as a final step before being used by the interpreter for which it is intended or by the interpreter itself. | Verify framework behavior | The only output-encoding surface is Jackson's JSON serialization, applied as the final step when writing the response body; the application writes no output through any other interpreter. |
| V1.2.1 | 1 | Verify that output encoding for an HTTP response, HTML document, or XML document is relevant for the context required, such as encoding the relevant characters for HTML elements, HTML attributes, HTML comments, CSS, or HTTP header fields, to avoid changing the message or document structure. | Verify framework behavior | The application renders no HTML or XML document of its own; its one HTML-producing surface, Spring Security's default login page, is generated and escaped by the framework. |
| V1.2.2 | 1 | Verify that when dynamically building URLs, untrusted data is encoded according to its context (e.g., URL encoding or base64url encoding for query or path parameters). Ensure that only safe URL protocols are permitted (e.g., disallow javascript: or data:). | Configured | The only dynamically built URIs are the `Location` header for a created resource (`URI.create("/admin/users/" + user.getId())`, using a server-generated ID) and the Keycloak account-endpoint URL (`AccountController`, built from the trusted, application-configured issuer URI). Neither concatenates user-supplied data into the URL. |
| V1.2.3 | 1 | Verify that output encoding or escaping is used when dynamically building JavaScript content (including JSON), to avoid changing the message or document structure (to avoid JavaScript and JSON injection). | Configured | All JSON output is produced by Jackson; the handful of hand-written JSON strings (`ContentNegotiatingSessionExpiredStrategy`, `ProblemDetailAccessDeniedHandler`, `TomcatConfiguration`'s error valve) interpolate only fixed, non-user-controlled values such as a status code or reason phrase. |
| V1.2.4 | 1 | Verify that data selection or database queries (e.g., SQL, HQL, NoSQL, Cypher) use parameterized queries, ORMs, entity frameworks, or are otherwise protected from SQL Injection and other database injection attacks. This is also relevant when writing stored procedures. | Configured | Every query in `AdministrationService` uses Spring Data JPA's repository methods or the Criteria API through `Specification`; no native or JPQL query string is built by concatenating user input. |
| V1.2.5 | 1 | Verify that the application protects against OS command injection and that operating system calls use parameterized OS queries or use contextual command line output encoding. | Not applicable to this template | The application never invokes an operating system command; there is no `ProcessBuilder` or `Runtime.exec` call anywhere in its code. |
| V1.2.6 | 2 | Verify that the application protects against LDAP injection vulnerabilities, or that specific security controls to prevent LDAP injection have been implemented. | Not applicable to this template | The application has no LDAP integration. |
| V1.2.7 | 2 | Verify that the application is protected against XPath injection attacks by using query parameterization or precompiled queries. | Not applicable to this template | The application performs no XPath queries. |
| V1.2.8 | 2 | Verify that LaTeX processors are configured securely (such as not using the "--shell-escape" flag) and an allowlist of commands is used to prevent LaTeX injection attacks. | Not applicable to this template | The application has no LaTeX processing. |
| V1.2.9 | 2 | Verify that the application escapes special characters in regular expressions (typically using a backslash) to prevent them from being misinterpreted as metacharacters. | Not applicable to this template | The application builds no regular expression from untrusted input; see V1.3.12 for the patterns it does apply to request-derived data. |
| V1.2.10 | 3 | Verify that the application is protected against CSV and Formula Injection. The application must follow the escaping rules defined in RFC 4180 sections 2.6 and 2.7 when exporting CSV content. Additionally, when exporting to CSV or other spreadsheet formats (such as XLS, XLSX, or ODF), special characters (including '=', '+', '-', '@', '\t' (tab), and '\0' (null character)) must be escaped with a single quote if they appear as the first character in a field value. | Not applicable to this template | The application has no CSV, spreadsheet, or other export feature. |
| V1.3.1 | 1 | Verify that all untrusted HTML input from WYSIWYG editors or similar is sanitized using a well-known and secure HTML sanitization library or framework feature. | Not applicable to this template | The application has no WYSIWYG editor or HTML-accepting input field; every admin field is plain text validated by Bean Validation constraints. |
| V1.3.2 | 1 | Verify that the application avoids the use of eval() or other dynamic code execution features such as Spring Expression Language (SpEL). Where there is no alternative, any user input being included must be sanitized before being executed. | Configured | The only Spring Expression Language usage is the `@PreAuthorize` annotations on the admin controllers, which are static expressions written by developers, never built from user input. The application uses no other dynamic code execution feature. |
| V1.3.3 | 2 | Verify that data being passed to a potentially dangerous context is sanitized beforehand to enforce safety measures, such as only allowing characters which are safe for this context and trimming input which is too long. | Not applicable to this template | No downstream context the application passes data to (a shell, a template engine, raw HTML) is dangerous in this sense; every admin field is already length- and format-constrained by Bean Validation. |
| V1.3.4 | 2 | Verify that user-supplied Scalable Vector Graphics (SVG) scriptable content is validated or sanitized to contain only tags and attributes (such as draw graphics) that are safe for the application, e.g., do not contain scripts and foreignObject. | Not applicable to this template | The application accepts no SVG or other scriptable graphics content. |
| V1.3.5 | 2 | Verify that the application sanitizes or disables user-supplied scriptable or expression template language content, such as Markdown, CSS or XSL stylesheets, BBCode, or similar. | Not applicable to this template | The application accepts no user-supplied Markdown, CSS, XSL, or similar scriptable content. |
| V1.3.6 | 2 | Verify that the application protects against Server-side Request Forgery (SSRF) attacks, by validating untrusted data against an allowlist of protocols, domains, paths and ports and sanitizing potentially dangerous characters before using the data to call another service. | Configured | The application's one outbound call, `AccountController` to Keycloak's account endpoint, targets a fixed, application-configured URI; it is never derived from request input, so there is no allowlist to validate against. |
| V1.3.7 | 2 | Verify that the application protects against template injection attacks by not allowing templates to be built based on untrusted input. Where there is no alternative, any untrusted input being included dynamically during template creation must be sanitized or strictly validated. | Not applicable to this template | The application uses no template engine; `spring-boot-starter-thymeleaf`, freemarker, and mustache are not dependencies. Its one HTML-producing surface, Spring Security's default login page, takes no user input into its output. |
| V1.3.8 | 2 | Verify that the application appropriately sanitizes untrusted input before use in Java Naming and Directory Interface (JNDI) queries and that JNDI is configured securely to prevent JNDI injection attacks. | Not applicable to this template | The application performs no JNDI lookups of its own. |
| V1.3.9 | 2 | Verify that the application sanitizes content before it is sent to memcache to prevent injection attacks. | Not applicable to this template | The application has no memcache integration. |
| V1.3.10 | 2 | Verify that format strings which might resolve in an unexpected or malicious way when used are sanitized before being processed. | Configured | The application's `String.formatted()` and structured logging calls always use a hardcoded format string; only argument values, never the format pattern itself, can come from request input. |
| V1.3.11 | 2 | Verify that the application sanitizes user input before passing to mail systems to protect against SMTP or IMAP injection. | Not applicable to this template | The application has no mail-sending integration. |
| V1.3.12 | 3 | Verify that regular expressions are free from elements causing exponential backtracking, and ensure untrusted input is sanitized to mitigate ReDoS or Runaway Regex attacks. | Configured | The only regular expressions applied to request-derived input, in `TrustedProxyMatcher` and `CloudFrontViewerAddressClientIpResolver`, are simple, linear-time character classes (such as `[0-9a-fA-F:.]+`) with no nested quantifiers that could backtrack catastrophically. |
| V1.4.1 | 2 | Verify that the application uses memory-safe string, safer memory copy and pointer arithmetic to detect or prevent stack, buffer, or heap overflows. | Verify framework behavior | Java is a memory-safe language: it has no raw pointer arithmetic, and array accesses are bounds-checked by the JVM. The application performs no unsafe memory operation. |
| V1.4.2 | 2 | Verify that sign, range, and input validation techniques are used to prevent integer overflows. | Verify framework behavior | The JVM traps integer overflow as defined arithmetic behavior rather than undefined behavior; the application performs no unchecked pointer arithmetic that an overflowing value could corrupt. |
| V1.4.3 | 2 | Verify that dynamically allocated memory and resources are released, and that references or pointers to freed memory are removed or set to null to prevent dangling pointers and use-after-free vulnerabilities. | Verify framework behavior | The JVM's garbage collector manages object lifetime; the application holds no raw pointer that could dangle or be freed twice. |
| V1.5.1 | 1 | Verify that the application configures XML parsers to use a restrictive configuration and that unsafe features such as resolving external entities are disabled to prevent XML eXternal Entity (XXE) attacks. | Not applicable to this template | The application parses no XML; it has no XML parser to configure. |
| V1.5.2 | 2 | Verify that deserialization of untrusted data enforces safe input handling, such as using an allowlist of object types or restricting client-defined object types, to prevent deserialization attacks. Deserialization mechanisms that are explicitly defined as insecure must not be used with untrusted input. | Configured | Jackson, Spring Boot's default JSON library, deserializes request bodies only into the exact declared record types (such as `UserCreateRequest`); the application enables no polymorphic or default typing that would let a request choose an arbitrary class to instantiate. |
| V1.5.3 | 3 | Verify that different parsers used in the application for the same data type (e.g., JSON parsers, XML parsers, URL parsers), perform parsing in a consistent way and use the same character encoding mechanism to avoid issues such as JSON Interoperability vulnerabilities or different URI or file parsing behavior being exploited in Remote File Inclusion (RFI) or Server-side Request Forgery (SSRF) attacks. | Configured | Jackson is the application's sole parser for JSON, its only structured input format; there is no second parser for the same data type whose behavior could diverge. |

## V2: Validation and Business Logic

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V2.1.1 | 1 | Verify that the application's documentation defines input validation rules for how to check the validity of data items against an expected structure. This could be common data formats such as credit card numbers, email addresses, telephone numbers, or it could be an internal data format. | Configured | The admin API's input fields are documented by self-describing Bean Validation annotations: `@Username`, `@DisplayName`, and `@ResourceName` (blank/length rules) and `@Email` (email format), applied to both the request DTOs and the JPA entities. |
| V2.1.2 | 2 | Verify that the application's documentation defines how to validate the logical and contextual consistency of combined data items, such as checking that suburb and ZIP code match. | Not applicable to this template | The admin API's fields (username, display name, email, group/role membership) have no cross-field consistency rule to define. Reassess if a domain model with related fields is added. |
| V2.1.3 | 2 | Verify that expectations for business logic limits and validations are documented, including both per-user and globally across the application. | Verification required | No per-user or global business-logic limits are documented; the base template has no business logic beyond administration CRUD. An adopter must define these for their own domain. |
| V2.2.1 | 1 | Verify that input is validated to enforce business or functional expectations for that input. This should either use positive validation against an allow list of values, patterns, and ranges, or be based on comparing the input to an expected structure and logical limits according to predefined rules. For L1, this can focus on input which is used to make specific business or security decisions. For L2 and up, this should apply to all input. | Configured | Every admin request DTO field is validated with Bean Validation constraints, and `AdminPageable` validates the `sort` parameter against an explicit allowlist of sortable properties rather than accepting an arbitrary column name. |
| V2.2.2 | 1 | Verify that the application is designed to enforce input validation at a trusted service layer. While client-side validation improves usability and should be encouraged, it must not be relied upon as a security control. | Configured | The application has no client-side validation to rely on at all; every constraint is enforced server-side via `@Valid`/`@Validated` on the admin controllers. |
| V2.2.3 | 2 | Verify that the application ensures that combinations of related data items are reasonable according to the pre-defined rules. | Not applicable to this template | The admin API's fields have no cross-field relationship to validate; see V2.1.2. |
| V2.3.1 | 1 | Verify that the application will only process business logic flows for the same user in the expected sequential step order and without skipping steps. | Not applicable to this template | Every admin operation (create, update, delete) is a single atomic request; there is no multi-step flow whose steps could be skipped or reordered. |
| V2.3.2 | 2 | Verify that business logic limits are implemented per the application's documentation to avoid business logic flaws being exploited. | Verification required | No business logic limits are implemented, since none are yet documented; see V2.1.3. |
| V2.3.3 | 2 | Verify that transactions are being used at the business logic level such that either a business logic operation succeeds in its entirety or it is rolled back to the previous correct state. | Configured | `AdministrationService` is annotated `@Transactional` at the class level, so every administration operation commits or rolls back atomically. |
| V2.3.4 | 2 | Verify that business logic level locking mechanisms are used to ensure that limited quantity resources (such as theater seats or delivery slots) cannot be double-booked by manipulating the application's logic. | Not applicable to this template | The base template has no limited-quantity-resource booking feature; the admin API's uniqueness checks (username, group name, role name) are a different concern, not a double-booking scenario. |
| V2.3.5 | 3 | Verify that high-value business logic flows require multi-user approval to prevent unauthorized or accidental actions. This could include but is not limited to large monetary transfers, contract approvals, access to classified information, or safety overrides in manufacturing. | Verification required | No multi-user approval or step-up mechanism exists for any administration action; this is the same open item recorded at V7.5.3. |
| V2.4.1 | 2 | Verify that anti-automation controls are in place to protect against excessive calls to application functions that could lead to data exfiltration, garbage-data creation, quota exhaustion, rate-limit breaches, denial-of-service, or overuse of costly resources. | Verification required | No rate limiting or anti-automation control is implemented anywhere in the application. An adopter must add this before exposing functions where it matters. |
| V2.4.2 | 3 | Verify that business logic flows require realistic human timing, preventing excessively rapid transaction submissions. | Verification required | No timing check exists on any administration action; the base template has no business logic flow this would apply to yet. |

## V3: Web Frontend Security

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V3.1.1 | 3 | Verify that application documentation states the expected security features that browsers using the application must support (such as HTTPS, HTTP Strict Transport Security (HSTS), Content Security Policy (CSP), and other relevant HTTP security mechanisms). It must also define how the application must behave when some of these features are not available (such as warning the user or blocking access). | Verification required | `headers.md` documents the expected security-feature values (HTTPS, HSTS, CSP, and the other headers below), but the application defines no behavior for a browser that does not support them; as a JSON API it has no rendered page to show a warning on. An adopter with a browser UI must decide and implement that behavior. |
| V3.2.1 | 1 | Verify that security controls are in place to prevent browsers from rendering content or functionality in HTTP responses in an incorrect context (e.g., when an API, a user-uploaded file or other resource is requested directly). Possible controls could include: not serving the content unless HTTP request header fields (such as Sec-Fetch-\*) indicate it is the correct context, using the sandbox directive of the Content-Security-Policy header field or using the attachment disposition type in the Content-Disposition header field. | Configured | Every response is `application/json` or `application/problem+json`, `X-Content-Type-Options: nosniff` is set (see `headers.md`), and the application serves no user-uploaded or dynamically rendered file content that could be requested out of context. |
| V3.2.2 | 1 | Verify that content intended to be displayed as text, rather than rendered as HTML, is handled using safe rendering functions (such as createTextNode or textContent) to prevent unintended execution of content such as HTML or JavaScript. | Not applicable to this template | The application has no client-side JavaScript; it has no bundled frontend. |
| V3.2.3 | 3 | Verify that the application avoids DOM clobbering when using client-side JavaScript by employing explicit variable declarations, performing strict type checking, avoiding storing global variables on the document object, and implementing namespace isolation. | Not applicable to this template | The application has no client-side JavaScript; it has no bundled frontend. |
| V3.3.1 | 1 | Verify that cookies have the 'Secure' attribute set, and if the '\__Host-' prefix is not used for the cookie name, the '__Secure-' prefix must be used for the cookie name. | Verification required | The session cookie has `Secure=true` outside the `local` and `test` profiles, but its name is the plain `id`, not `__Host-id` or `__Secure-id`; see `headers.md` ("Cookie name prefix": Improvement) for the reasoning to confirm before adopting a prefix. |
| V3.3.2 | 2 | Verify that each cookie's 'SameSite' attribute value is set according to the purpose of the cookie, to limit exposure to user interface redress attacks and browser-based request forgery attacks, commonly known as cross-site request forgery (CSRF). | Configured | `SameSite=Lax` is deliberately set, chosen over `Strict` so the OIDC redirect from Keycloak still carries the pre-login session cookie; see `headers.md` ("Set-Cookie"). |
| V3.3.3 | 2 | Verify that cookies have the '__Host-' prefix for the cookie name unless they are explicitly designed to be shared with other hosts. | Verification required | The cookie name is `id`, not `__Host-id`; see the same gap and reasoning recorded at V3.3.1 and `headers.md` ("Cookie name prefix"). |
| V3.3.4 | 2 | Verify that if the value of a cookie is not meant to be accessible to client-side scripts (such as a session token), the cookie must have the 'HttpOnly' attribute set and the same value (e. g. session token) must only be transferred to the client via the 'Set-Cookie' header field. | Configured | The session cookie has `HttpOnly=true`, and `tracking-modes: COOKIE` disables URL-based session propagation, so the session value is never exposed to client-side script or a rewritten URL; see `headers.md` ("Set-Cookie"). |
| V3.3.5 | 3 | Verify that when the application writes a cookie, the cookie name and value length combined are not over 4096 bytes. Overly large cookies will not be stored by the browser and therefore not sent with requests, preventing the user from using application functionality which relies on that cookie. | Configured | The cookie carries only an opaque, short Spring Session identifier under the name `id`, far below the 4096-byte combined limit. |
| V3.4.1 | 1 | Verify that a Strict-Transport-Security header field is included on all responses to enforce an HTTP Strict Transport Security (HSTS) policy. A maximum age of at least 1 year must be defined, and for L2 and up, the policy must apply to all subdomains as well. | Configured | Spring Security's default HSTS header (`max-age=31536000; includeSubDomains`) is emitted on every secure response; see `headers.md` ("Strict-Transport-Security"). It is correctly absent over the `local`/`test` profiles' plain HTTP, since HSTS has no meaning there. |
| V3.4.2 | 1 | Verify that the Cross-Origin Resource Sharing (CORS) Access-Control-Allow-Origin header field is a fixed value by the application, or if the Origin HTTP request header field value is used, it is validated against an allowlist of trusted origins. When 'Access-Control-Allow-Origin: *' needs to be used, verify that the response does not include any sensitive information. | Configured | No CORS policy is configured, so browsers apply the same-origin default and no `Access-Control-Allow-Origin` header is emitted at all; see `headers.md`. |
| V3.4.3 | 2 | Verify that HTTP responses include a Content-Security-Policy response header field which defines directives to ensure the browser only loads and executes trusted content or resources, in order to limit execution of malicious JavaScript. As a minimum, a global policy must be used which includes the directives object-src 'none' and base-uri 'none' and defines either an allowlist or uses nonces or hashes. For an L3 application, a per-response policy with nonces or hashes must be defined. | Configured | The global policy is `base-uri 'none'; default-src 'none'; form-action 'none'; frame-ancestors 'none'`. `default-src 'none'` already covers `object-src` by CSP fallback, and because nothing may load at all, a per-response nonce or hash allowlist would add nothing; see `headers.md` ("Content-Security-Policy"). |
| V3.4.4 | 2 | Verify that all HTTP responses contain an 'X-Content-Type-Options: nosniff' header field. This instructs browsers not to use content sniffing and MIME type guessing for the given response, and to require the response's Content-Type header field value to match the destination resource. For example, the response to a request for a style is only accepted if the response's Content-Type is 'text/css'. This also enables the use of the Cross-Origin Read Blocking (CORB) functionality by the browser. | Configured | `X-Content-Type-Options: nosniff` is set by Spring Security's default header writer on every response; see `headers.md`. |
| V3.4.5 | 2 | Verify that the application sets a referrer policy to prevent leakage of technically sensitive data to third-party services via the 'Referer' HTTP request header field. This can be done using the Referrer-Policy HTTP response header field or via HTML element attributes. Sensitive data could include path and query data in the URL, and for internal non-public applications also the hostname. | Configured | `Referrer-Policy: strict-origin-when-cross-origin` is explicitly set; see `headers.md`. |
| V3.4.6 | 2 | Verify that the web application uses the frame-ancestors directive of the Content-Security-Policy header field for every HTTP response to ensure that it cannot be embedded by default and that embedding of specific resources is allowed only when necessary. Note that the X-Frame-Options header field, although supported by browsers, is obsolete and may not be relied upon. | Configured | The CSP includes `frame-ancestors 'none'` on every response, stricter than embedding-with-exceptions; see `headers.md`. |
| V3.4.7 | 3 | Verify that the Content-Security-Policy header field specifies a location to report violations. | Verification required | No CSP `report-to` or `report-uri` directive is configured; see `headers.md` ("Content-Security-Policy"). This is an L3-only enhancement not yet adopted. |
| V3.4.8 | 3 | Verify that all HTTP responses that initiate a document rendering (such as responses with Content-Type text/html), include the Cross‑Origin‑Opener‑Policy header field with the same-origin directive or the same-origin-allow-popups directive as required. This prevents attacks that abuse shared access to Window objects, such as tabnabbing and frame counting. | Verification required | `Cross-Origin-Opener-Policy` is not set; see `headers.md`, which records that it needs evaluation against the OAuth redirect flow before enabling and is generally not useful for JSON API responses. It would still apply to the one HTML-rendering response this application produces, the default login page. |
| V3.5.1 | 1 | Verify that, if the application does not rely on the CORS preflight mechanism to prevent disallowed cross-origin requests to use sensitive functionality, these requests are validated to ensure they originate from the application itself. This may be done by using and validating anti-forgery tokens or requiring extra HTTP header fields that are not CORS-safelisted request-header fields. This is to defend against browser-based request forgery attacks, commonly known as cross-site request forgery (CSRF). | Configured | Spring Security's default CSRF protection issues and validates a token independent of CORS, and `WebSecurityConfigurationTest.csrfFailureReturnsActionableProblemDetail` covers the rejection path. |
| V3.5.2 | 1 | Verify that, if the application relies on the CORS preflight mechanism to prevent disallowed cross-origin use of sensitive functionality, it is not possible to call the functionality with a request which does not trigger a CORS-preflight request. This may require checking the values of the 'Origin' and 'Content-Type' request header fields or using an extra header field that is not a CORS-safelisted header-field. | Not applicable to this template | The application defends against CSRF with tokens (see V3.5.1), not by relying on the CORS preflight mechanism, so this alternative control does not apply. |
| V3.5.3 | 1 | Verify that HTTP requests to sensitive functionality use appropriate HTTP methods such as POST, PUT, PATCH, or DELETE, and not methods defined by the HTTP specification as "safe" such as HEAD, OPTIONS, or GET. Alternatively, strict validation of the Sec-Fetch-* request header fields can be used to ensure that the request did not originate from an inappropriate cross-origin call, a navigation request, or a resource load (such as an image source) where this is not expected. | Configured | Every state-changing admin endpoint uses POST, PUT, or DELETE (`UserAdminController`, `GroupAdminController`, `RoleAdminController`); GET is used only for reads. |
| V3.5.4 | 2 | Verify that separate applications are hosted on different hostnames to leverage the restrictions provided by same-origin policy, including how documents or scripts loaded by one origin can interact with resources from another origin and hostname-based restrictions on cookies. | Deployment decision required | Whether this application shares a hostname with another application is a deployment topology decision the template cannot make. |
| V3.5.5 | 2 | Verify that messages received by the postMessage interface are discarded if the origin of the message is not trusted, or if the syntax of the message is invalid. | Not applicable to this template | The application has no client-side JavaScript and does not use the `postMessage` interface. |
| V3.5.6 | 3 | Verify that JSONP functionality is not enabled anywhere across the application to avoid Cross-Site Script Inclusion (XSSI) attacks. | Configured | No JSONP mechanism (callback-wrapped JSON, `ResponseBodyAdvice`, or similar) exists anywhere in the codebase; every response is plain JSON. |
| V3.5.7 | 3 | Verify that data requiring authorization is not included in script resource responses, like JavaScript files, to prevent Cross-Site Script Inclusion (XSSI) attacks. | Not applicable to this template | The application serves no static script resources of its own. |
| V3.5.8 | 3 | Verify that authenticated resources (such as images, videos, scripts, and other documents) can be loaded or embedded on behalf of the user only when intended. This can be accomplished by strict validation of the Sec-Fetch-* HTTP request header fields to ensure that the request did not originate from an inappropriate cross-origin call, or by setting a restrictive Cross-Origin-Resource-Policy HTTP response header field to instruct the browser to block returned content. | Verification required | No `Cross-Origin-Resource-Policy` header is set; see `headers.md`, which notes the application does not currently serve resources intended to be embedded elsewhere. A browser's default same-origin read restriction already blocks a simple cross-origin fetch of authenticated JSON; CORP would add defense-in-depth against side-channel leaks. |
| V3.6.1 | 3 | Verify that client-side assets, such as JavaScript libraries, CSS, or web fonts, are only hosted externally (e.g., on a Content Delivery Network) if the resource is static and versioned and Subresource Integrity (SRI) is used to validate the integrity of the asset. If this is not possible, there should be a documented security decision to justify this for each resource. | Not applicable to this template | The application loads no externally-hosted client-side assets; it has no bundled frontend. |
| V3.7.1 | 2 | Verify that the application only uses client-side technologies which are still supported and considered secure. Examples of technologies which do not meet this requirement include NSAPI plugins, Flash, Shockwave, ActiveX, Silverlight, NACL, or client-side Java applets. | Not applicable to this template | The application uses no client-side technology of any kind; it has no bundled frontend. |
| V3.7.2 | 2 | Verify that the application will only automatically redirect the user to a different hostname or domain (which is not controlled by the application) where the destination appears on an allowlist. | Configured | Every application-initiated redirect targets a fixed, application-configured URI: the Keycloak authorization and end-session endpoints from the client registration, or the `{baseUrl}/login?logout` post-logout URI. None is derived from unvalidated user input. |
| V3.7.3 | 3 | Verify that the application shows a notification when the user is being redirected to a URL outside of the application's control, with an option to cancel the navigation. | Verification required | The application has no rendered UI to show such a notification; its redirects are plain HTTP 302 responses to the fixed destinations described at V3.7.2. An adopter with a browser UI that redirects to a genuinely external, adopter-controlled destination must add this. |
| V3.7.4 | 3 | Verify that the application's top-level domain (e.g., site.tld) is added to the public preload list for HTTP Strict Transport Security (HSTS). This ensures that the use of TLS for the application is built directly into the main browsers, rather than relying only on the Strict-Transport-Security response header field. | Deployment decision required | Submitting the application's production domain to the HSTS preload list is a deployment decision made once the domain and its HTTPS posture are finalized. |
| V3.7.5 | 3 | Verify that the application behaves as documented (such as warning the user or blocking access) if the browser used to access the application does not support the expected security features. | Verification required | No behavior is defined or implemented for a browser lacking an expected security feature; see the same gap recorded at V3.1.1. |

## V4: API and Web Service

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V4.1.1 | 1 | Verify that every HTTP response with a message body contains a Content-Type header field that matches the actual content of the response, including the charset parameter to specify safe character encoding (e.g., UTF-8, ISO-8859-1) according to IANA Media Types, such as "text/", "/+xml" and "/xml". | Configured | Spring's message converters set `Content-Type: application/json` (or `application/problem+json`) on every response body, matching the actual JSON content; JSON's own specification (RFC 8259) mandates UTF-8, so no separate charset parameter is needed. |
| V4.1.2 | 2 | Verify that only user-facing endpoints (intended for manual web-browser access) automatically redirect from HTTP to HTTPS, while other services or endpoints do not implement transparent redirects. This is to avoid a situation where a client is erroneously sending unencrypted HTTP requests, but since the requests are being automatically redirected to HTTPS, the leakage of sensitive data goes undiscovered. | Deployment decision required | Whether and how plain HTTP requests are redirected to HTTPS, and whether that differs between browser-facing and other endpoints, is decided by the reverse proxy or load balancer terminating TLS in front of this application; the application's own connector does not implement such a redirect. |
| V4.1.3 | 2 | Verify that any HTTP header field used by the application and set by an intermediary layer, such as a load balancer, a web proxy, or a backend-for-frontend service, cannot be overridden by the end-user. Example headers might include X-Real-IP, X-Forwarded-*, or X-User-ID. | Configured | `ClientIpResolver.none()` is the default bean, so no intermediary header is trusted by default. `TrustedHeaderClientIpResolver`, `XForwardedForClientIpResolver`, and `CloudFrontViewerAddressClientIpResolver` only honor `X-Forwarded-For`/`CloudFront-Viewer-Address` when the immediate peer address matches a configured trusted-proxy CIDR, so an end user cannot forge these headers to spoof a client address once a resolver is wired up. |
| V4.1.4 | 3 | Verify that only HTTP methods that are explicitly supported by the application or its API (including OPTIONS during preflight requests) can be used and that unused methods are blocked. | Configured | Spring MVC only registers a handler for a mapping's declared HTTP methods; any other method on a mapped path returns 405 through `ApiResponseEntityExceptionHandler.handleHttpRequestMethodNotSupported` (tested in `ApiResponseEntityExceptionHandlerTest.methodNotAllowedReturnsProblemDetail`), and `TomcatConfiguration` explicitly disables TRACE at the connector. |
| V4.1.5 | 3 | Verify that per-message digital signatures are used to provide additional assurance on top of transport protections for requests or transactions which are highly sensitive or which traverse a number of systems. | Verification required | No per-message digital signature scheme exists for the admin API; TLS provides transport-level integrity only. An adopter with a highly sensitive, multi-hop transaction must add this. |
| V4.2.1 | 2 | Verify that all application components (including load balancers, firewalls, and application servers) determine boundaries of incoming HTTP messages using the appropriate mechanism for the HTTP version to prevent HTTP request smuggling. In HTTP/1.x, if a Transfer-Encoding header field is present, the Content-Length header must be ignored per RFC 2616. When using HTTP/2 or HTTP/3, if a Content-Length header field is present, the receiver must ensure that it is consistent with the length of the DATA frames. | Verify framework behavior | Determining HTTP message boundaries and rejecting inconsistent `Transfer-Encoding`/`Content-Length` combinations is the embedded Tomcat server's responsibility, not application code. Keep Tomcat current and verify any reverse proxy in front of it applies the same framing rules. |
| V4.2.2 | 3 | Verify that when generating HTTP messages, the Content-Length header field does not conflict with the length of the content as determined by the framing of the HTTP protocol, in order to prevent request smuggling attacks. | Verify framework behavior | Generating consistent `Content-Length` framing is handled by the servlet container and Spring MVC's response writing; the application does not construct raw HTTP framing itself. |
| V4.2.3 | 3 | Verify that the application does not send nor accept HTTP/2 or HTTP/3 messages with connection-specific header fields such as Transfer-Encoding to prevent response splitting and header injection attacks. | Verify framework behavior | Rejecting connection-specific header fields on HTTP/2 or HTTP/3 is the embedded Tomcat server's responsibility; the application does not set or read `Transfer-Encoding` itself. |
| V4.2.4 | 3 | Verify that the application only accepts HTTP/2 and HTTP/3 requests where the header fields and values do not contain any CR (\r), LF (\n), or CRLF (\r\n) sequences, to prevent header injection attacks. | Verify framework behavior | HTTP/2 and HTTP/3's binary, length-prefixed header encoding is handled entirely by the embedded Tomcat server; the application has no code path that could inject a raw CR or LF into a header value. |
| V4.2.5 | 3 | Verify that, if the application (backend or frontend) builds and sends requests, it uses validation, sanitization, or other mechanisms to avoid creating URIs (such as for API calls) or HTTP request header fields (such as Authorization or Cookie), which are too long to be accepted by the receiving component. This could cause a denial of service, such as when sending an overly long request (e.g., a long cookie header field), which results in the server always responding with an error status. | Configured | The only outbound request this application builds is `AccountController`'s call to Keycloak's account endpoint, using a fixed, short URI with no unbounded user-controlled data concatenated into it or its headers. |
| V4.3.1 | 2 | Verify that a query allowlist, depth limiting, amount limiting, or query cost analysis is used to prevent GraphQL or data layer expression Denial of Service (DoS) as a result of expensive, nested queries. | Not applicable to this template | The application exposes a plain REST API, not GraphQL; `spring-boot-starter-graphql` is not a dependency. |
| V4.3.2 | 2 | Verify that GraphQL introspection queries are disabled in the production environment unless the GraphQL API is meant to be used by other parties. | Not applicable to this template | The application exposes a plain REST API, not GraphQL; `spring-boot-starter-graphql` is not a dependency. |
| V4.4.1 | 1 | Verify that WebSocket over TLS (WSS) is used for all WebSocket connections. | Not applicable to this template | The application has no WebSocket support; `spring-boot-starter-websocket` is not a dependency. |
| V4.4.2 | 2 | Verify that, during the initial HTTP WebSocket handshake, the Origin header field is checked against a list of origins allowed for the application. | Not applicable to this template | The application has no WebSocket support; `spring-boot-starter-websocket` is not a dependency. |
| V4.4.3 | 2 | Verify that, if the application's standard session management cannot be used, dedicated tokens are being used for this, which comply with the relevant Session Management security requirements. | Not applicable to this template | The application has no WebSocket support; `spring-boot-starter-websocket` is not a dependency. |
| V4.4.4 | 2 | Verify that dedicated WebSocket session management tokens are initially obtained or validated through the previously authenticated HTTPS session when transitioning an existing HTTPS session to a WebSocket channel. | Not applicable to this template | The application has no WebSocket support; `spring-boot-starter-websocket` is not a dependency. |

## V5: File Handling

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V5.1.1 | 2 | Verify that the documentation defines the permitted file types, expected file extensions, and maximum size (including unpacked size) for each upload feature. Additionally, ensure that the documentation specifies how files are made safe for end-users to download and process, such as how the application behaves when a malicious file is detected. | Not applicable to this template | The application has no file upload or download capability of any kind; no controller accepts `multipart/form-data` or serves a file response. Reassess before adding a file-handling feature. |
| V5.2.1 | 1 | Verify that the application will only accept files of a size which it can process without causing a loss of performance or a denial of service attack. | Not applicable to this template | The application has no file upload or download capability of any kind; no controller accepts `multipart/form-data` or serves a file response. Reassess before adding a file-handling feature. |
| V5.2.2 | 1 | Verify that when the application accepts a file, either on its own or within an archive such as a zip file, it checks if the file extension matches an expected file extension and validates that the contents correspond to the type represented by the extension. This includes, but is not limited to, checking the initial 'magic bytes', performing image re-writing, and using specialized libraries for file content validation. For L1, this can focus just on files which are used to make specific business or security decisions. For L2 and up, this must apply to all files being accepted. | Not applicable to this template | The application has no file upload or download capability of any kind; no controller accepts `multipart/form-data` or serves a file response. Reassess before adding a file-handling feature. |
| V5.2.3 | 2 | Verify that the application checks compressed files (e.g., zip, gz, docx, odt) against maximum allowed uncompressed size and against maximum number of files before uncompressing the file. | Not applicable to this template | The application has no file upload or download capability of any kind; no controller accepts `multipart/form-data` or serves a file response. Reassess before adding a file-handling feature. |
| V5.2.4 | 3 | Verify that a file size quota and maximum number of files per user are enforced to ensure that a single user cannot fill up the storage with too many files, or excessively large files. | Not applicable to this template | The application has no file upload or download capability of any kind; no controller accepts `multipart/form-data` or serves a file response. Reassess before adding a file-handling feature. |
| V5.2.5 | 3 | Verify that the application does not allow uploading compressed files containing symlinks unless this is specifically required (in which case it will be necessary to enforce an allowlist of the files that can be symlinked to). | Not applicable to this template | The application has no file upload or download capability of any kind; no controller accepts `multipart/form-data` or serves a file response. Reassess before adding a file-handling feature. |
| V5.2.6 | 3 | Verify that the application rejects uploaded images with a pixel size larger than the maximum allowed, to prevent pixel flood attacks. | Not applicable to this template | The application has no file upload or download capability of any kind; no controller accepts `multipart/form-data` or serves a file response. Reassess before adding a file-handling feature. |
| V5.3.1 | 1 | Verify that files uploaded or generated by untrusted input and stored in a public folder, are not executed as server-side program code when accessed directly with an HTTP request. | Not applicable to this template | The application has no file upload or download capability of any kind; no controller accepts `multipart/form-data` or serves a file response. Reassess before adding a file-handling feature. |
| V5.3.2 | 1 | Verify that when the application creates file paths for file operations, instead of user-submitted filenames, it uses internally generated or trusted data, or if user-submitted filenames or file metadata must be used, strict validation and sanitization must be applied. This is to protect against path traversal, local or remote file inclusion (LFI, RFI), and server-side request forgery (SSRF) attacks. | Not applicable to this template | The application has no file upload or download capability of any kind; no controller accepts `multipart/form-data` or serves a file response. Reassess before adding a file-handling feature. |
| V5.3.3 | 3 | Verify that server-side file processing, such as file decompression, ignores user-provided path information to prevent vulnerabilities such as zip slip. | Not applicable to this template | The application has no file upload or download capability of any kind; no controller accepts `multipart/form-data` or serves a file response. Reassess before adding a file-handling feature. |
| V5.4.1 | 2 | Verify that the application validates or ignores user-submitted filenames, including in a JSON, JSONP, or URL parameter and specifies a filename in the Content-Disposition header field in the response. | Not applicable to this template | The application has no file upload or download capability of any kind; no controller accepts `multipart/form-data` or serves a file response. Reassess before adding a file-handling feature. |
| V5.4.2 | 2 | Verify that file names served (e.g., in HTTP response header fields or email attachments) are encoded or sanitized (e.g., following RFC 6266) to preserve document structure and prevent injection attacks. | Not applicable to this template | The application has no file upload or download capability of any kind; no controller accepts `multipart/form-data` or serves a file response. Reassess before adding a file-handling feature. |
| V5.4.3 | 2 | Verify that files obtained from untrusted sources are scanned by antivirus scanners to prevent serving of known malicious content. | Not applicable to this template | The application has no file upload or download capability of any kind; no controller accepts `multipart/form-data` or serves a file response. Reassess before adding a file-handling feature. |

## V6: Authentication

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V6.1.1 | 1 | Verify that application documentation defines how controls such as rate limiting, anti-automation, and adaptive response, are used to defend against attacks such as credential stuffing and password brute force. The documentation must make clear how these controls are configured and prevent malicious account lockout. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.1.2 | 2 | Verify that a list of context-specific words is documented in order to prevent their use in passwords. The list could include permutations of organization names, product names, system identifiers, project codenames, department or role names, and similar. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.1.3 | 2 | Verify that, if the application includes multiple authentication pathways, these are all documented together with the security controls and authentication strength which must be consistently enforced across them. | Not applicable to this template | The application has exactly one authentication pathway: OIDC via Keycloak. There is no second pathway whose consistency with it would need documenting. |
| V6.2.1 | 1 | Verify that user set passwords are at least 8 characters in length although a minimum of 15 characters is strongly recommended. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.2.2 | 1 | Verify that users can change their password. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.2.3 | 1 | Verify that password change functionality requires the user's current and new password. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.2.4 | 1 | Verify that passwords submitted during account registration or password change are checked against an available set of, at least, the top 3000 passwords which match the application's password policy, e.g. minimum length. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.2.5 | 1 | Verify that passwords of any composition can be used, without rules limiting the type of characters permitted. There must be no requirement for a minimum number of upper or lower case characters, numbers, or special characters. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.2.6 | 1 | Verify that password input fields use type=password to mask the entry. Applications may allow the user to temporarily view the entire masked password, or the last typed character of the password. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.2.7 | 1 | Verify that "paste" functionality, browser password helpers, and external password managers are permitted. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.2.8 | 1 | Verify that the application verifies the user's password exactly as received from the user, without any modifications such as truncation or case transformation. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.2.9 | 2 | Verify that passwords of at least 64 characters are permitted. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.2.10 | 2 | Verify that a user's password stays valid until it is discovered to be compromised or the user rotates it. The application must not require periodic credential rotation. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.2.11 | 2 | Verify that the documented list of context specific words is used to prevent easy to guess passwords being created. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.2.12 | 2 | Verify that passwords submitted during account registration or password changes are checked against a set of breached passwords. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.3.1 | 1 | Verify that controls to prevent attacks such as credential stuffing and password brute force are implemented according to the application's security documentation. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.3.2 | 1 | Verify that default user accounts (e.g., "root", "admin", or "sa") are not present in the application or are disabled. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.3.3 | 2 | Verify that either a multi-factor authentication mechanism or a combination of single-factor authentication mechanisms, must be used in order to access the application. For L3, one of the factors must be a hardware-based authentication mechanism which provides compromise and impersonation resistance against phishing attacks while verifying the intent to authenticate by requiring a user-initiated action (such as a button press on a FIDO hardware key or a mobile phone). Relaxing any of the considerations in this requirement requires a fully documented rationale and a comprehensive set of mitigating controls. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.3.4 | 2 | Verify that, if the application includes multiple authentication pathways, there are no undocumented pathways and that security controls and authentication strength are enforced consistently. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.3.5 | 3 | Verify that users are notified of suspicious authentication attempts (successful or unsuccessful). This may include authentication attempts from an unusual location or client, partially successful authentication (only one of multiple factors), an authentication attempt after a long period of inactivity or a successful authentication after several unsuccessful attempts. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.3.6 | 3 | Verify that email is not used as either a single-factor or multi-factor authentication mechanism. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.3.7 | 3 | Verify that users are notified after updates to authentication details, such as credential resets or modification of the username or email address. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.3.8 | 3 | Verify that valid users cannot be deduced from failed authentication challenges, such as by basing on error messages, HTTP response codes, or different response times. Registration and forgot password functionality must also have this protection. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.4.1 | 1 | Verify that system generated initial passwords or activation codes are securely randomly generated, follow the existing password policy, and expire after a short period of time or after they are initially used. These initial secrets must not be permitted to become the long term password. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.4.2 | 1 | Verify that password hints or knowledge-based authentication (so-called "secret questions") are not present. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.4.3 | 2 | Verify that a secure process for resetting a forgotten password is implemented, that does not bypass any enabled multi-factor authentication mechanisms. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.4.4 | 2 | Verify that if a multi-factor authentication factor is lost, evidence of identity proofing is performed at the same level as during enrollment. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.4.5 | 3 | Verify that renewal instructions for authentication mechanisms which expire are sent with enough time to be carried out before the old authentication mechanism expires, configuring automated reminders if necessary. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.4.6 | 3 | Verify that administrative users can initiate the password reset process for the user, but that this does not allow them to change or choose the user's password. This prevents a situation where they know the user's password. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.5.1 | 2 | Verify that lookup secrets, out-of-band authentication requests or codes, and time-based one-time passwords (TOTPs) are only successfully usable once. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.5.2 | 2 | Verify that, when being stored in the application's backend, lookup secrets with less than 112 bits of entropy (19 random alphanumeric characters or 34 random digits) are hashed with an approved password storage hashing algorithm that incorporates a 32-bit random salt. A standard hash function can be used if the secret has 112 bits of entropy or more. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.5.3 | 2 | Verify that lookup secrets, out-of-band authentication code, and time-based one-time password seeds, are generated using a Cryptographically Secure Pseudorandom Number Generator (CSPRNG) to avoid predictable values. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.5.4 | 2 | Verify that lookup secrets and out-of-band authentication codes have a minimum of 20 bits of entropy (typically 4 random alphanumeric characters or 6 random digits is sufficient). | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.5.5 | 2 | Verify that out-of-band authentication requests, codes, or tokens, as well as time-based one-time passwords (TOTPs) have a defined lifetime. Out of band requests must have a maximum lifetime of 10 minutes and for TOTP a maximum lifetime of 30 seconds. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.5.6 | 3 | Verify that any authentication factor (including physical devices) can be revoked in case of theft or other loss. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.5.7 | 3 | Verify that biometric authentication mechanisms are only used as secondary factors together with either something you have or something you know. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.5.8 | 3 | Verify that time-based one-time passwords (TOTPs) are checked based on a time source from a trusted service and not from an untrusted or client provided time. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.6.1 | 2 | Verify that authentication mechanisms using the Public Switched Telephone Network (PSTN) to deliver One-time Passwords (OTPs) via phone or SMS are offered only when the phone number has previously been validated, alternate stronger methods (such as Time based One-time Passwords) are also offered, and the service provides information on their security risks to users. For L3 applications, phone and SMS must not be available as options. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.6.2 | 2 | Verify that out-of-band authentication requests, codes, or tokens are bound to the original authentication request for which they were generated and are not usable for a previous or subsequent one. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.6.3 | 2 | Verify that a code based out-of-band authentication mechanism is protected against brute force attacks by using rate limiting. Consider also using a code with at least 64 bits of entropy. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.6.4 | 3 | Verify that, where push notifications are used for multi-factor authentication, rate limiting is used to prevent push bombing attacks. Number matching may also mitigate this risk. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.7.1 | 3 | Verify that the certificates used to verify cryptographic authentication assertions are stored in a way protects them from modification. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.7.2 | 3 | Verify that the challenge nonce is at least 64 bits in length, and statistically unique or unique over the lifetime of the cryptographic device. | Deployment decision required | Authentication is delegated to Keycloak. Configure and verify this requirement in the identity provider and its operational procedures. |
| V6.8.1 | 2 | Verify that, if the application supports multiple identity providers (IdPs), the user's identity cannot be spoofed via another supported identity provider (eg. by using the same user identifier). The standard mitigation would be for the application to register and identify the user using a combination of the IdP ID (serving as a namespace) and the user's ID in the IdP. | Not applicable to this template | The application has a single OAuth2 client registration (`keycloak`) and interacts with only one identity provider, so cross-IdP identity spoofing does not apply. This is the same fact recorded at V10.2.2. Reassess if a second identity provider is ever registered. |
| V6.8.2 | 2 | Verify that the presence and integrity of digital signatures on authentication assertions (for example on JWTs or SAML assertions) are always validated, rejecting any assertions that are unsigned or have invalid signatures. | Configured | `WebSecurityConfiguration` validates ID-token signatures using an RS256 JWS selector restricted to signature-use keys; keep this covered by integration tests. |
| V6.8.3 | 2 | Verify that SAML assertions are uniquely processed and used only once within the validity period to prevent replay attacks. | Not applicable to this template | The application uses OIDC exclusively; it processes no SAML assertion. |
| V6.8.4 | 2 | Verify that, if an application uses a separate Identity Provider (IdP) and expects specific authentication strength, methods, or recentness for specific functions, the application verifies this using the information returned by the IdP. For example, if OIDC is used, this might be achieved by validating ID Token claims such as 'acr', 'amr', and 'auth_time' (if present). If the IdP does not provide this information, the application must have a documented fallback approach that assumes that the minimum strength authentication mechanism was used (for example, single-factor authentication using username and password). | Verification required | The application validates no `acr`, `amr`, or `auth_time` claim and defines no function requiring specific authentication strength; this is the same gap recorded at V7.6.1. There is also no documented fallback assumption for when the IdP omits this information. |

## V7: Session Management

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V7.1.1 | 2 | Verify that the user's session inactivity timeout and absolute maximum session lifetime are documented, are appropriate in combination with other controls, and that the documentation includes justification for any deviations from NIST SP 800-63B re-authentication requirements. | Verification required | The 15-minute idle timeout and 12-hour absolute timeout are documented in [Sessions](sessions.md#current-configuration). A written justification for these specific values, including any deviation from NIST SP 800-63B re-authentication requirements, is not yet recorded; see [Sessions](sessions.md#required-production-decisions) item 1. |
| V7.1.2 | 2 | Verify that the documentation defines how many concurrent (parallel) sessions are allowed for one account as well as the intended behaviors and actions to be taken when the maximum number of active sessions is reached. | Configured | [Sessions](sessions.md#owasp-review) documents the concurrent-session policy: one session per account, with a later login expiring the existing session. `WebSecurityConfiguration` configures `maximumSessions(1)` with `maxSessionsPreventsLogin(false)` and `ContentNegotiatingSessionExpiredStrategy`; `WebSecurityConfigurationSessionManagementIntegrationTest` verifies the behavior. |
| V7.1.3 | 2 | Verify that all systems that create and manage user sessions as part of a federated identity management ecosystem (such as SSO systems) are documented along with controls to coordinate session lifetimes, termination, and any other conditions that require re-authentication. | Verification required | Keycloak back-channel logout is configured and documented ([Authentication](authentication.md#back-channel-logout)), coordinating IdP-initiated logout with the local session. Broader session-lifetime coordination, such as requiring re-authentication when the maximum time since the last IdP authentication event is reached, is not implemented; see [Sessions](sessions.md#required-production-decisions) item 3. |
| V7.2.1 | 1 | Verify that the application performs all session token verification using a trusted, backend service. | Configured | Session tokens are opaque cookie values verified server-side against JDBC-backed session storage (`JdbcIndexedSessionRepository`, via `spring-boot-starter-session-jdbc`); see [Sessions](sessions.md#design-and-ownership). No self-contained or client-verifiable token is used for session state. |
| V7.2.2 | 1 | Verify that the application uses either self-contained or reference tokens that are dynamically generated for session management, i.e. not using static API secrets and keys. | Configured | Spring Session dynamically generates a new opaque session identifier per session; the application configures no static API secret or key for session management. See [Sessions](sessions.md#owasp-review) ("Opaque, unpredictable server-generated IDs"). |
| V7.2.3 | 1 | Verify that if reference tokens are used to represent user sessions, they are unique and generated using a cryptographically secure pseudo-random number generator (CSPRNG) and possess at least 128 bits of entropy. | Verify framework behavior | Session-identifier generation, its CSPRNG source, and its entropy are guaranteed by Spring Session's `JdbcIndexedSessionRepository`, not by template code. Keep the dependency current and recheck after upgrades. |
| V7.2.4 | 1 | Verify that the application generates a new session token on user authentication, including re-authentication, and terminates the current session token. | Configured | Spring Security's default session-fixation protection issues a new session ID on authentication and removes the prior JDBC session row. `WebSecurityConfigurationSessionManagementIntegrationTest` verifies both the ID change and the row removal; see [Sessions](sessions.md#owasp-review) ("Rotate ID on authentication"). |
| V7.3.1 | 2 | Verify that there is an inactivity timeout such that re-authentication is enforced according to risk analysis and documented security decisions. | Configured | The 15-minute idle timeout is server-enforced via `server.servlet.session.timeout`; see [Sessions](sessions.md#current-configuration). |
| V7.3.2 | 2 | Verify that there is an absolute maximum session lifetime such that re-authentication is enforced according to risk analysis and documented security decisions. | Configured | `AbsoluteSessionTimeoutFilter` invalidates a session once it reaches the configured 12-hour absolute timeout, regardless of activity; see [Sessions](sessions.md#current-configuration). |
| V7.4.1 | 1 | Verify that when session termination is triggered (such as logout or expiration), the application disallows any further use of the session. For reference tokens or stateful sessions, this means invalidating the session data at the application backend. Applications using self-contained tokens will need a solution such as maintaining a list of terminated tokens, disallowing tokens produced before a per-user date and time or rotating a per-user signing key. | Configured | Logout, the 12-hour absolute timeout, and concurrent-session expiry all invalidate the JDBC session row so the terminated session cannot be reused; see `AbsoluteSessionTimeoutFilter`, `SessionLifecycleLogoutHandler`, `ContentNegotiatingSessionExpiredStrategy`, and [Sessions](sessions.md#owasp-review). |
| V7.4.2 | 1 | Verify that the application terminates all active sessions when a user account is disabled or deleted (such as an employee leaving the company). | Configured | Disabling or deleting a user through the administration API revokes that user's active sessions via `SessionRevocationService`, which looks up their sessions through `SessionRegistry` and expires them immediately; the user's next request is treated as a concurrent-session expiry (see V7.1.2). This covers the affected user's own account; it does not cascade to other users when a shared group's roles change or a role is deleted (see V8.3.2). |
| V7.4.3 | 2 | Verify that the application gives the option to terminate all other active sessions after a successful change or removal of any authentication factor (including password change via reset or recovery and, if present, an MFA settings update). | Configured | Only one concurrent session is permitted per account (`maximumSessions(1)`). A login following a password or MFA change at the identity provider automatically expires the prior session as a side effect of this policy, without needing a dedicated "terminate other sessions" feature. |
| V7.4.4 | 2 | Verify that all pages that require authentication have easy and visible access to logout functionality. | Verification required | The template renders no browser UI of its own, so it defines no authenticated pages with a logout control to evaluate. To verify this requirement, an adopter must confirm that every authenticated page or view in their consuming frontend provides easy, visible access to the application's `/logout` endpoint. |
| V7.4.5 | 2 | Verify that application administrators are able to terminate active sessions for an individual user or for all users. | Verification required | An administrator can force an individual user's session to end by disabling them through `UserAdminController`, which triggers `SessionRevocationService` (see V7.4.2). There is no dedicated session-termination endpoint that leaves the account enabled, and no capability to terminate sessions for all users at once. An adopter needing either must add it. |
| V7.5.1 | 2 | Verify that the application requires full re-authentication before allowing modifications to sensitive account attributes which may affect authentication such as email address, phone number, MFA configuration, or other information used in account recovery. | Verification required | The template has no self-service flow where a user modifies their own account attributes; email and similar fields are changed only by an administrator acting on another user through the role-gated administration API (see [Authorization](authorization.md)). To verify this requirement, an adopter who adds a self-service profile flow must require re-authentication before it accepts changes to sensitive attributes. |
| V7.5.2 | 2 | Verify that users are able to view and (having authenticated again with at least one factor) terminate any or all currently active sessions. | Verification required | No endpoint lets a user view or terminate their own active sessions. An adopter needing this must add and verify it directly. |
| V7.5.3 | 3 | Verify that the application requires further authentication with at least one factor or secondary verification before performing highly sensitive transactions or operations. | Verification required | No step-up or secondary-verification mechanism exists before any operation; see [Authentication](authentication.md#owasp-review) ("Require re-authentication for sensitive features and after risk events": Not implemented). An adopter must define and implement this before exposing highly sensitive operations. |
| V7.6.1 | 2 | Verify that session lifetime and termination between Relying Parties (RPs) and Identity Providers (IdPs) behave as documented, requiring re-authentication as necessary such as when the maximum time between IdP authentication events is reached. | Verification required | Keycloak back-channel logout is the only implemented coordination between the identity provider's session and the local session (see [Authentication](authentication.md#back-channel-logout)). The application does not track or enforce a maximum time since the last IdP authentication event (for example via the ID token's `auth_time` claim) to trigger re-authentication; see [Sessions](sessions.md#required-production-decisions) item 3. |
| V7.6.2 | 2 | Verify that creation of a session requires either the user's consent or an explicit action, preventing the creation of new application sessions without user interaction. | Verify framework behavior | Any request to a protected route creates a session before the user takes any explicit action, because Spring Security's OAuth2 login mechanism needs a place to store the pending authorization request across the redirect to and from the identity provider. This is inherent to the standard OAuth2 authorization-code flow, not a template-specific choice; verify it has not changed after a Spring Security upgrade. |

## V8: Authorization

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V8.1.1 | 1 | Verify that authorization documentation defines rules for restricting function-level and data-specific access based on consumer permissions and resource attributes. | Verification required | [Authorization](authorization.md) documents function-level rules for the template's own admin API (its "Management authority boundary" table), but the template defines no data-specific or resource-attribute rules, because its administered resources (users, groups, roles) are not owned by individual callers. An adopter who adds a domain model needing per-record or attribute-based access rules must define and implement those rules themselves. |
| V8.1.2 | 2 | Verify that authorization documentation defines rules for field-level access restrictions (both read and write) based on consumer permissions and resource attributes. Note that these rules might depend on other attribute values of the relevant data object, such as state or status. | Verification required | The template requires authentication but does not define product authorization rules. Implement and test policy at the service and data layers. |
| V8.1.3 | 3 | Verify that the application's documentation defines the environmental and contextual attributes (including but not limited to, time of day, user location, IP address, or device) that are used in the application to make security decisions, including those pertaining to authentication and authorization. | Verification required | The template requires authentication but does not define product authorization rules. Implement and test policy at the service and data layers. |
| V8.1.4 | 3 | Verify that authentication and authorization documentation defines how environmental and contextual factors are used in decision-making, in addition to function-level, data-specific, and field-level authorization. This should include the attributes evaluated, thresholds for risk, and actions taken (e.g., allow, challenge, deny, step-up authentication). | Verification required | The template requires authentication but does not define product authorization rules. Implement and test policy at the service and data layers. |
| V8.2.1 | 1 | Verify that the application ensures that function-level access is restricted to consumers with explicit permissions. | Configured | `WebSecurityConfiguration` restricts `/admin/users/**`, `/admin/groups/**`, and `/admin/roles/**` to the matching management authority, and `@PreAuthorize` repeats the same check on `UserAdminController`, `GroupAdminController`, and `RoleAdminController`; every other route requires authentication. `AdminControllerTest` covers the user-management authority; group and role administration have no equivalent regression test yet. |
| V8.2.2 | 1 | Verify that the application ensures that data-specific access is restricted to consumers with explicit permissions to specific data items to mitigate insecure direct object reference (IDOR) and broken object level authorization (BOLA). | Verification required | The template has no per-record ownership model to protect: its administered resources are gated by role, not by caller ownership, and the only self-scoped endpoints (`/login-user`, `/account`) take no ID and always return the caller's own data. An adopter who adds a domain model where a non-admin caller can reach a specific record by ID must implement and test IDOR/BOLA protections for it. |
| V8.2.3 | 2 | Verify that the application ensures that field-level access is restricted to consumers with explicit permissions to specific fields to mitigate broken object property level authorization (BOPLA). | Verification required | The template requires authentication but does not define product authorization rules. Implement and test policy at the service and data layers. |
| V8.2.4 | 3 | Verify that adaptive security controls based on a consumer's environmental and contextual attributes (such as time of day, location, IP address, or device) are implemented for authentication and authorization decisions, as defined in the application's documentation. These controls must be applied when the consumer tries to start a new session and also during an existing session. | Verification required | The template requires authentication but does not define product authorization rules. Implement and test policy at the service and data layers. |
| V8.3.1 | 1 | Verify that the application enforces authorization rules at a trusted service layer and doesn't rely on controls that an untrusted consumer could manipulate, such as client-side JavaScript. | Configured | The application has no client-rendered frontend. Every authorization decision is enforced server-side by Spring Security (`@PreAuthorize` and `authorizeHttpRequests`). |
| V8.3.2 | 3 | Verify that changes to values on which authorization decisions are made are applied immediately. Where changes cannot be applied immediately, (such as when relying on data in self-contained tokens), there must be mitigating controls to alert when a consumer performs an action when they are no longer authorized to do so and revert the change. Note that this alternative would not mitigate information leakage. | Verification required | Disabling a user or changing their own group membership now revokes their session immediately via `SessionRevocationService` (see V7.4.2), so those two changes take effect right away instead of only at the user's next authentication. Redefining a group's roles or deleting a role does not cascade to the sessions of every affected member, so those changes still only take effect at each affected user's next authentication, with no mechanism to detect or alert on an action taken with now-stale authorities in between. See [Authorization](authorization.md#security-behaviour). |
| V8.3.3 | 3 | Verify that access to an object is based on the originating subject's (e.g. consumer's) permissions, not on the permissions of any intermediary or service acting on their behalf. For example, if a consumer calls a web service using a self-contained token for authentication, and the service then requests data from a different service, the second service will use the consumer's token, rather than a machine-to-machine token from the first service, to make permission decisions. | Configured | `AccountController` retrieves the Keycloak account resource using the calling user's own `OAuth2AuthorizedClient` access token, obtained through `RestClientConfiguration`'s `OAuth2ClientHttpRequestInterceptor`, not a shared service-level credential. This is the only outbound call the application makes on a user's behalf. |
| V8.4.1 | 2 | Verify that multi-tenant applications use cross-tenant controls to ensure consumer operations will never affect tenants with which they do not have permissions to interact. | Verification required | The template requires authentication but does not define product authorization rules. Implement and test policy at the service and data layers. |
| V8.4.2 | 3 | Verify that access to administrative interfaces incorporates multiple layers of security, including continuous consumer identity verification, device security posture assessment, and contextual risk analysis, ensuring that network location or trusted endpoints are not the sole factors for authorization even though they may reduce the likelihood of unauthorized access. | Verification required | The template requires authentication but does not define product authorization rules. Implement and test policy at the service and data layers. |

## V9: Self-contained Tokens

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V9.1.1 | 1 | Verify that self-contained tokens are validated using their digital signature or MAC to protect against tampering before accepting the token's contents. | Configured | The template validates ID-token signatures before accepting claims. Assess any additional self-contained token use separately. |
| V9.1.2 | 1 | Verify that only algorithms on an allowlist can be used to create and verify self-contained tokens, for a given context. The allowlist must include the permitted algorithms, ideally only either symmetric or asymmetric algorithms, and must not include the 'None' algorithm. If both symmetric and asymmetric must be supported, additional controls will be needed to prevent key confusion. | Configured | The ID-token decoder explicitly selects RS256. Assess any additional self-contained token use separately. |
| V9.1.3 | 1 | Verify that key material that is used to validate self-contained tokens is from trusted pre-configured sources for the token issuer, preventing attackers from specifying untrusted sources and keys. For JWTs and other JWS structures, headers such as 'jku', 'x5u', and 'jwk' must be validated against an allowlist of trusted sources. | Configured | The ID-token decoder resolves keys from the fixed, pre-configured issuer JWKS URI (`clientRegistration.getProviderDetails().getJwkSetUri()`), never from an attacker-suppliable `jku`, `x5u`, or `jwk` header. |
| V9.2.1 | 1 | Verify that, if a validity time span is present in the token data, the token and its content are accepted only if the verification time is within this validity time span. For example, for JWTs, the claims 'nbf' and 'exp' must be verified. | Verify framework behavior | `OidcIdTokenValidator` validates the ID token's `exp`, `iat`, and, if present, `nbf` claims; see `WebSecurityConfigurationTest.oidcIdTokenValidatorRejectsUnexpectedIssuerAudienceAndAuthorizedParty`. |
| V9.2.2 | 2 | Verify that the service receiving a token validates the token to be the correct type and is meant for the intended purpose before accepting the token's contents. For example, only access tokens can be accepted for authorization decisions and only ID Tokens can be used for proving user authentication. | Configured | The custom `JwtDecoderFactory<ClientRegistration>` only ever decodes ID tokens for authentication; the application never accepts an access token in its place. |
| V9.2.3 | 2 | Verify that the service only accepts tokens which are intended for use with that service (audience). For JWTs, this can be achieved by validating the 'aud' claim against an allowlist defined in the service. | Configured | `OidcIdTokenValidator` validates the ID token's `aud` claim against the client registration's `client-id`; see `WebSecurityConfigurationTest.oidcIdTokenValidatorRejectsUnexpectedIssuerAudienceAndAuthorizedParty`. This duplicates V10.5.4. |
| V9.2.4 | 2 | Verify that, if a token issuer uses the same private key for issuing tokens to different audiences, the issued tokens contain an audience restriction that uniquely identifies the intended audiences. This will prevent a token from being reused with an unintended audience. If the audience identifier is dynamically provisioned, the token issuer must validate these audiences in order to make sure that they do not result in audience impersonation. | Deployment decision required | Keycloak, as the token issuer, decides whether it signs ID tokens for multiple clients with the same key and how it scopes their audience restrictions; this application only validates the resulting `aud` claim (V9.2.3, V10.5.4). |

## V10: OAuth and OIDC

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V10.1.1 | 2 | Verify that tokens are only sent to components that strictly need them. For example, when using a backend-for-frontend pattern for browser-based JavaScript applications, access and refresh tokens shall only be accessible for the backend. | Configured | Access and refresh tokens are held only in the server-side, JDBC-backed session via Spring Security's default `OAuth2AuthorizedClientRepository`; the browser receives only the opaque session cookie. See [Sessions](sessions.md#design-and-ownership). No component other than this backend ever receives the tokens. |
| V10.1.2 | 2 | Verify that the client only accepts values from the authorization server (such as the authorization code or ID Token) if these values result from an authorization flow that was initiated by the same user agent session and transaction. This requires that client-generated secrets, such as the proof key for code exchange (PKCE) 'code_verifier', 'state' or OIDC 'nonce', are not guessable, are specific to the transaction, and are securely bound to both the client and the user agent session in which the transaction was started. | Verify framework behavior | Spring Security's OAuth2 client stores the pending authorization request, including `state` and the OIDC `nonce`, in the same JDBC-backed session used for authentication, generated by the framework and bound to the user agent's session. This client authenticates as a confidential client (`private_key_jwt`), so Spring Security does not add PKCE, which it reserves for public clients. Verify this behavior after a Spring Security upgrade. |
| V10.2.1 | 2 | Verify that, if the code flow is used, the OAuth client has protection against browser-based request forgery attacks, commonly known as cross-site request forgery (CSRF), which trigger token requests, either by using proof key for code exchange (PKCE) functionality or checking the 'state' parameter that was sent in the authorization request. | Verify framework behavior | Spring Security validates the `state` parameter returned by Keycloak against the value stored in the user's authorization request before exchanging the authorization code, preventing a forged callback from triggering a token request. Verify this behavior after a Spring Security upgrade. |
| V10.2.2 | 2 | Verify that, if the OAuth client can interact with more than one authorization server, it has a defense against mix-up attacks. For example, it could require that the authorization server return the 'iss' parameter value and validate it in the authorization response and the token response. | Not applicable to this template | The application has a single OAuth2 client registration (`keycloak`) and interacts with only one authorization server, so a mix-up attack between multiple authorization servers does not apply. Reassess if a second identity provider is ever registered. |
| V10.2.3 | 3 | Verify that the OAuth client only requests the required scopes (or other authorization parameters) in requests to the authorization server. | Configured | The `keycloak` client registration requests only the `openid` scope; see `application.yaml` (`spring.security.oauth2.client.registration.keycloak.scope`). |
| V10.3.1 | 2 | Verify that the resource server only accepts access tokens that are intended for use with that service (audience). The audience may be included in a structured access token (such as the 'aud' claim in JWT), or it can be checked using the token introspection endpoint. | Not applicable to this template | The application has no OAuth2 Resource Server role: it is a session-based relying party with no Bearer-token-authenticated endpoint, and `spring-boot-starter-oauth2-resource-server` is not a dependency. Reassess if the template is extended to accept access tokens directly from another client. |
| V10.3.2 | 2 | Verify that the resource server enforces authorization decisions based on claims from the access token that define delegated authorization. If claims such as 'sub', 'scope', and 'authorization_details' are present, they must be part of the decision. | Not applicable to this template | The application has no OAuth2 Resource Server role: it is a session-based relying party with no Bearer-token-authenticated endpoint, and `spring-boot-starter-oauth2-resource-server` is not a dependency. Reassess if the template is extended to accept access tokens directly from another client. |
| V10.3.3 | 2 | Verify that if an access control decision requires identifying a unique user from an access token (JWT or related token introspection response), the resource server identifies the user from claims that cannot be reassigned to other users. Typically, it means using a combination of 'iss' and 'sub' claims. | Not applicable to this template | The application has no OAuth2 Resource Server role: it is a session-based relying party with no Bearer-token-authenticated endpoint, and `spring-boot-starter-oauth2-resource-server` is not a dependency. Reassess if the template is extended to accept access tokens directly from another client. |
| V10.3.4 | 2 | Verify that, if the resource server requires specific authentication strength, methods, or recentness, it verifies that the presented access token satisfies these constraints. For example, if present, using the OIDC 'acr', 'amr' and 'auth_time' claims respectively. | Not applicable to this template | The application has no OAuth2 Resource Server role: it is a session-based relying party with no Bearer-token-authenticated endpoint, and `spring-boot-starter-oauth2-resource-server` is not a dependency. Reassess if the template is extended to accept access tokens directly from another client. |
| V10.3.5 | 3 | Verify that the resource server prevents the use of stolen access tokens or replay of access tokens (from unauthorized parties) by requiring sender-constrained access tokens, either Mutual TLS for OAuth 2 or OAuth 2 Demonstration of Proof of Possession (DPoP). | Not applicable to this template | The application has no OAuth2 Resource Server role: it is a session-based relying party with no Bearer-token-authenticated endpoint, and `spring-boot-starter-oauth2-resource-server` is not a dependency. Reassess if the template is extended to accept access tokens directly from another client. |
| V10.4.1 | 1 | Verify that the authorization server validates redirect URIs based on a client-specific allowlist of pre-registered URIs using exact string comparison. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.4.2 | 1 | Verify that, if the authorization server returns the authorization code in the authorization response, it can be used only once for a token request. For the second valid request with an authorization code that has already been used to issue an access token, the authorization server must reject a token request and revoke any issued tokens related to the authorization code. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.4.3 | 1 | Verify that the authorization code is short-lived. The maximum lifetime can be up to 10 minutes for L1 and L2 applications and up to 1 minute for L3 applications. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.4.4 | 1 | Verify that for a given client, the authorization server only allows the usage of grants that this client needs to use. Note that the grants 'token' (Implicit flow) and 'password' (Resource Owner Password Credentials flow) must no longer be used. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.4.5 | 1 | Verify that the authorization server mitigates refresh token replay attacks for public clients, preferably using sender-constrained refresh tokens, i.e., Demonstrating Proof of Possession (DPoP) or Certificate-Bound Access Tokens using mutual TLS (mTLS). For L1 and L2 applications, refresh token rotation may be used. If refresh token rotation is used, the authorization server must invalidate the refresh token after usage, and revoke all refresh tokens for that authorization if an already used and invalidated refresh token is provided. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.4.6 | 2 | Verify that, if the code grant is used, the authorization server mitigates authorization code interception attacks by requiring proof key for code exchange (PKCE). For authorization requests, the authorization server must require a valid 'code_challenge' value and must not accept a 'code_challenge_method' value of 'plain'. For a token request, it must require validation of the 'code_verifier' parameter. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.4.7 | 2 | Verify that if the authorization server supports unauthenticated dynamic client registration, it mitigates the risk of malicious client applications. It must validate client metadata such as any registered URIs, ensure the user's consent, and warn the user before processing an authorization request with an untrusted client application. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.4.8 | 2 | Verify that refresh tokens have an absolute expiration, including if sliding refresh token expiration is applied. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.4.9 | 2 | Verify that refresh tokens and reference access tokens can be revoked by an authorized user using the authorization server user interface, to mitigate the risk of malicious clients or stolen tokens. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.4.10 | 2 | Verify that confidential client is authenticated for client-to-authorized server backchannel requests such as token requests, pushed authorization requests (PAR), and token revocation requests. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.4.11 | 2 | Verify that the authorization server configuration only assigns the required scopes to the OAuth client. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.4.12 | 3 | Verify that for a given client, the authorization server only allows the 'response_mode' value that this client needs to use. For example, by having the authorization server validate this value against the expected values or by using pushed authorization request (PAR) or JWT-secured Authorization Request (JAR). | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.4.13 | 3 | Verify that grant type 'code' is always used together with pushed authorization requests (PAR). | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.4.14 | 3 | Verify that the authorization server issues only sender-constrained (Proof-of-Possession) access tokens, either with certificate-bound access tokens using mutual TLS (mTLS) or DPoP-bound access tokens (Demonstration of Proof of Possession). | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.4.15 | 3 | Verify that, for a server-side client (which is not executed on the end-user device), the authorization server ensures that the 'authorization_details' parameter value is from the client backend and that the user has not tampered with it. For example, by requiring the usage of pushed authorization request (PAR) or JWT-secured Authorization Request (JAR). | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.4.16 | 3 | Verify that the client is confidential and the authorization server requires the use of strong client authentication methods (based on public-key cryptography and resistant to replay attacks), such as mutual TLS ('tls_client_auth', 'self_signed_tls_client_auth') or private key JWT ('private_key_jwt'). | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.5.1 | 2 | Verify that the client (as the relying party) mitigates ID Token replay attacks. For example, by ensuring that the 'nonce' claim in the ID Token matches the 'nonce' value sent in the authentication request to the OpenID Provider (in OAuth2 refereed to as the authorization request sent to the authorization server). | Verify framework behavior | Spring Security's OIDC client manages the authorization request and nonce. Verify the effective behavior after framework upgrades. |
| V10.5.2 | 2 | Verify that the client uniquely identifies the user from ID Token claims, usually the 'sub' claim, which cannot be reassigned to other users (for the scope of an identity provider). | Verification required | The application identifies the local user from the OIDC `preferred_username` claim, not `sub` (see [Authorization](authorization.md#identity-resolution)). README.md instructs operators to treat Keycloak usernames as immutable once provisioned, but this is an operational policy, not a guarantee Keycloak enforces the way it enforces `sub` uniqueness. If a Keycloak administrator ever reassigned a `preferred_username` to a different person, this application would silently attach that identity to the wrong local user. An adopter who cannot enforce username immutability as an operational control should identify users by `sub` (and `iss`) instead. |
| V10.5.3 | 2 | Verify that the client rejects attempts by a malicious authorization server to impersonate another authorization server through authorization server metadata. The client must reject authorization server metadata if the issuer URL in the authorization server metadata does not exactly match the pre-configured issuer URL expected by the client. | Verify framework behavior | Spring Security obtains provider metadata from the configured issuer. Verify issuer/metadata matching in an end-to-end test for the selected provider. |
| V10.5.4 | 2 | Verify that the client validates that the ID Token is intended to be used for that client (audience) by checking that the 'aud' claim from the token is equal to the 'client_id' value for the client. | Configured | `OidcIdTokenValidator` validates the ID-token audience against the client registration. |
| V10.5.5 | 2 | Verify that, when using OIDC back-channel logout, the relying party mitigates denial of service through forced logout and cross-JWT confusion in the logout flow. The client must verify that the logout token is correctly typed with a value of 'logout+jwt', contains the 'event' claim with the correct member name, and does not contain a 'nonce' claim. Note that it is also recommended to have a short expiration (e.g., 2 minutes). | Verify framework behavior | Spring Security's `oidcLogout().backChannel()` support validates the logout token per the OpenID Connect Back-Channel Logout specification, including its type, `events` claim, and absence of `nonce`. See [Authentication](authentication.md#back-channel-logout) for the Keycloak-side configuration. Verify this behavior after a Spring Security upgrade. |
| V10.6.1 | 2 | Verify that the OpenID Provider only allows values 'code', 'ciba', 'id_token', or 'id_token code' for response mode. Note that 'code' is preferred over 'id_token code' (the OIDC Hybrid flow), and 'token' (any Implicit flow) must not be used. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.6.2 | 2 | Verify that the OpenID Provider mitigates denial of service through forced logout. By obtaining explicit confirmation from the end-user or, if present, validating parameters in the logout request (initiated by the relying party), such as the 'id_token_hint'. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.7.1 | 2 | Verify that the authorization server ensures that the user consents to each authorization request. If the identity of the client cannot be assured, the authorization server must always explicitly prompt the user for consent. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.7.2 | 2 | Verify that when the authorization server prompts for user consent, it presents sufficient and clear information about what is being consented to. When applicable, this should include the nature of the requested authorizations (typically based on scope, resource server, Rich Authorization Requests (RAR) authorization details), the identity of the authorized application, and the lifetime of these authorizations. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |
| V10.7.3 | 2 | Verify that the user can review, modify, and revoke consents which the user has granted through the authorization server. | Not applicable to this template | The template is an OAuth/OIDC relying party, not an authorization server, OpenID Provider, or consent-management service. Configure this in Keycloak if the deployment owns that role. |

## V11: Cryptography

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V11.1.1 | 2 | Verify that there is a documented policy for management of cryptographic keys and a cryptographic key lifecycle that follows a key management standard such as NIST SP 800-57. This should include ensuring that keys are not overshared (for example, with more than two entities for shared secrets and more than one entity for private keys). | Verification required | The one cryptographic asset this application manages directly, the JWKS signing/encryption key pair used for `private_key_jwt`, has documented handling rules: [ADR 0006](../adr/0006-tls-and-oauth-client-key-management.md) and [Authentication](authentication.md) require production key material to be supplied through protected deployment configuration, never embedded in source, and rotated in coordination with Keycloak. This is not framed against a formal standard such as NIST SP 800-57, and TLS certificate/private-key lifecycle is a separate deployment decision. |
| V11.1.2 | 2 | Verify that a cryptographic inventory is performed, maintained, regularly updated, and includes all cryptographic keys, algorithms, and certificates used by the application. It must also document where keys can and cannot be used in the system, and the types of data that can and cannot be protected using the keys. | Verification required | The application's cryptographic surface is documented, but not consolidated into one inventory: the JWKS signing (`ES512`) and encryption (`RSA-OAEP-256`) keys and their handling rules are in [Authentication](authentication.md); the TLS protocol versions and cipher suites are in `application.yaml` and [Hardening](hardening.md). An adopter should consolidate these into a single inventory before a formal audit. |
| V11.1.3 | 3 | Verify that cryptographic discovery mechanisms are employed to identify all instances of cryptography in the system, including encryption, hashing, and signing operations. | Verification required | No cryptographic-discovery tooling is configured. The application's only cryptographic operations, TLS and JWKS-based JWT signing, are enumerated manually in [Authentication](authentication.md) and [Hardening](hardening.md). |
| V11.1.4 | 3 | Verify that a cryptographic inventory is maintained. This must include a documented plan that outlines the migration path to new cryptographic standards, such as post-quantum cryptography, in order to react to future threats. | Verification required | No migration plan to post-quantum cryptography exists. The base template's cryptographic footprint is limited to TLS and JWKS-based signing, both of which depend entirely on the JDK's and Keycloak's own PQC support once available. |
| V11.2.1 | 2 | Verify that industry-validated implementations (including libraries and hardware-accelerated implementations) are used for cryptographic operations. | Configured | All cryptographic operations use industry-validated implementations: the JDK's own TLS stack for `server.ssl`, and Nimbus JOSE+JWT for JWKS handling, JWT signing, and ID-token validation. The application implements no cryptographic primitive itself. |
| V11.2.2 | 2 | Verify that the application is designed with crypto agility such that random number, authenticated encryption, MAC, or hashing algorithms, key lengths, rounds, ciphers and modes can be reconfigured, upgraded, or swapped at any time, to protect against cryptographic breaks. Similarly, it must also be possible to replace keys and passwords and re-encrypt data. This will allow for seamless upgrades to post-quantum cryptography (PQC), once high-assurance implementations of approved PQC schemes or standards are widely available. | Verification required | TLS cipher suites are a configurable list in `application.yaml`, and the JWKS key material is loaded from an external, replaceable resource (`app.jwks`), so both can be rotated or strengthened without a code change. The ID-token JWS algorithm is hardcoded to RS256 in `WebSecurityConfiguration.jwtDecoderFactory()`; changing it requires a code change, not just configuration. |
| V11.2.3 | 2 | Verify that all cryptographic primitives utilize a minimum of 128-bits of security based on the algorithm, key size, and configuration. For example, a 256-bit ECC key provides roughly 128 bits of security where RSA requires a 3072-bit key to achieve 128 bits of security. | Configured | The configured TLS cipher suites are exclusively AES-128-GCM or AES-256-GCM with ECDHE/DHE or TLS 1.3 key exchange, meeting or exceeding a 128-bit security level; see `application.yaml`. JWKS key sizing for a production deployment is covered by the key-management decision at V11.1.1. |
| V11.2.4 | 3 | Verify that all cryptographic operations are constant-time, with no 'short-circuit' operations in comparisons, calculations, or returns, to avoid leaking information. | Verify framework behavior | The application performs no comparison of secret or cryptographic material itself; TLS and JWT signature verification are handled entirely by the JDK and Nimbus JOSE+JWT, which are responsible for constant-time behavior internally. |
| V11.2.5 | 3 | Verify that all cryptographic modules fail securely, and errors are handled in a way that does not enable vulnerabilities, such as Padding Oracle attacks. | Verify framework behavior | The application implements no cryptographic module of its own; TLS and JWT failure handling are the JDK's and Nimbus JOSE+JWT's responsibility. |
| V11.3.1 | 1 | Verify that insecure block modes (e.g., ECB) and weak padding schemes (e.g., PKCS#1 v1.5) are not used. | Configured | TLS cipher suites are exclusively AEAD (AES-GCM), never a block mode like ECB or a legacy padding scheme; the JWKS encryption key specifies `RSA-OAEP-256`, not the legacy `PKCS#1 v1.5`; see `application.yaml` and `jwks.json`. |
| V11.3.2 | 1 | Verify that only approved ciphers and modes such as AES with GCM are used. | Configured | `application.yaml` explicitly configures only AES-GCM cipher suites (`server.ssl.ciphers`); no non-AEAD cipher is enabled. |
| V11.3.3 | 2 | Verify that encrypted data is protected against unauthorized modification preferably by using an approved authenticated encryption method or by combining an approved encryption method with an approved MAC algorithm. | Configured | The only encrypted-data channel this application manages is TLS, which uses AEAD (AES-GCM) cipher suites exclusively; see `application.yaml`. The application performs no separate, non-AEAD encryption of stored or transmitted data that would need a combined MAC. |
| V11.3.4 | 3 | Verify that nonces, initialization vectors, and other single-use numbers are not used for more than one encryption key and data-element pair. The method of generation must be appropriate for the algorithm being used. | Verify framework behavior | TLS's AEAD nonce management is handled internally by the JDK's TLS implementation; the application generates no initialization vector or nonce of its own. |
| V11.3.5 | 3 | Verify that any combination of an encryption algorithm and a MAC algorithm is operating in encrypt-then-MAC mode. | Not applicable to this template | The application never manually combines a separate encryption algorithm with a separate MAC algorithm; every encrypted channel it uses (TLS, JWKS `RSA-OAEP-256`) is AEAD or otherwise self-authenticating. |
| V11.4.1 | 1 | Verify that only approved hash functions are used for general cryptographic use cases, including digital signatures, HMAC, KDF, and random bit generation. Disallowed hash functions, such as MD5, must not be used for any cryptographic purpose. | Configured | The only signature algorithms in use, RS256 (RSA with SHA-256) and ES512 (ECDSA with SHA-512), both use SHA-2 family hash functions. No MD5 or SHA-1 is used anywhere in the application. |
| V11.4.2 | 2 | Verify that passwords are stored using an approved, computationally intensive, key derivation function (also known as a "password hashing function"), with parameter settings configured based on current guidance. The settings should balance security and performance to make brute-force attacks sufficiently challenging for the required level of security. | Not applicable to this template | The application never receives, stores, or hashes a password; credential storage is Keycloak's responsibility. See [Authentication](authentication.md). |
| V11.4.3 | 2 | Verify that hash functions used in digital signatures, as part of data authentication or data integrity are collision resistant and have appropriate bit-lengths. If collision resistance is required, the output length must be at least 256 bits. If only resistance to second pre-image attacks is required, the output length must be at least 128 bits. | Configured | RS256 uses a 256-bit SHA-256 digest and ES512 uses a 512-bit SHA-512 digest, both meeting the 256-bit collision-resistance threshold for signature use. |
| V11.4.4 | 2 | Verify that the application uses approved key derivation functions with key stretching parameters when deriving secret keys from passwords. The parameters in use must balance security and performance to prevent brute-force attacks from compromising the resulting cryptographic key. | Not applicable to this template | The application never derives a cryptographic key from a password; it never receives one. See [Authentication](authentication.md). |
| V11.5.1 | 2 | Verify that all random numbers and strings which are intended to be non-guessable must be generated using a cryptographically secure pseudo-random number generator (CSPRNG) and have at least 128 bits of entropy. Note that UUIDs do not respect this condition. | Verify framework behavior | The one non-guessable value whose entropy matters for security is the session identifier, generated by Spring Session's `JdbcIndexedSessionRepository` (see V7.2.3). The application's own `UUID.randomUUID()` calls (the audit session identifier, the default request correlation ID) are log-correlation labels, not access-granting secrets, so a plain UUID's entropy is not a security concern for them. |
| V11.5.2 | 3 | Verify that the random number generation mechanism in use is designed to work securely, even under heavy demand. | Verify framework behavior | Random-number generation under load is the JDK's `SecureRandom` and Nimbus JOSE+JWT's responsibility; the application implements no random number generator of its own. |
| V11.6.1 | 2 | Verify that only approved cryptographic algorithms and modes of operation are used for key generation and seeding, and digital signature generation and verification. Key generation algorithms must not generate insecure keys vulnerable to known attacks, for example, RSA keys which are vulnerable to Fermat factorization. | Deployment decision required | The JWKS signing and encryption keys are generated outside this application, by whatever process creates the deployment-supplied JWKS; see [Authentication](authentication.md). Ensuring that process does not produce weak or factorable keys is a deployment decision the template cannot make. |
| V11.6.2 | 3 | Verify that approved cryptographic algorithms are used for key exchange (such as Diffie-Hellman) with a focus on ensuring that key exchange mechanisms use secure parameters. This will prevent attacks on the key establishment process which could lead to adversary-in-the-middle attacks or cryptographic breaks. | Configured | TLS key exchange is restricted to ECDHE, DHE, and TLS 1.3's own ephemeral exchange, all of which provide forward secrecy; see `application.yaml`. The JDK's TLS implementation selects the negotiated group parameters. |
| V11.7.1 | 3 | Verify that full memory encryption is in use that protects sensitive data while it is in use, preventing access by unauthorized users or processes. | Deployment decision required | Full memory encryption is a hardware or hypervisor capability outside the scope of the application; it is an infrastructure decision for the deployment platform. |
| V11.7.2 | 3 | Verify that data minimization ensures the minimal amount of data is exposed during processing, and ensure that data is encrypted immediately after use or as soon as feasible. | Verification required | The application's data-minimization posture depends on the data an adopter's domain model introduces; see V14 for the same open item. Its own surface is limited to OIDC claims held in the session and the user-directory fields exposed by the administration API, neither of which the template currently classifies as requiring encryption at rest beyond the database's own protection. |

## V12: Secure Communication

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V12.1.1 | 1 | Verify that only the latest recommended versions of the TLS protocol are enabled, such as TLS 1.2 and TLS 1.3. The latest version of the TLS protocol must be the preferred option. | Configured | The production profile enables TLS 1.2 and TLS 1.3 only. Local and test profiles intentionally differ and must not be deployed. |
| V12.1.2 | 2 | Verify that only recommended cipher suites are enabled, with the strongest cipher suites set as preferred. L3 applications must only support cipher suites which provide forward secrecy. | Configured | `application.yaml` explicitly configures modern AEAD cipher suites. Reassess against the deployed JDK and organizational baseline. |
| V12.1.3 | 2 | Verify that the application validates that mTLS client certificates are trusted before using the certificate identity for authentication or authorization. | Not applicable to this template | The application does not implement mTLS; it authenticates browser clients through OIDC and its own outbound call through OAuth2, not client certificates. Reassess if mTLS is introduced. |
| V12.1.4 | 3 | Verify that proper certification revocation, such as Online Certificate Status Protocol (OCSP) Stapling, is enabled and configured. | Deployment decision required | OCSP stapling depends on the production certificate authority and JVM TLS configuration, neither of which the template supplies; `server.ssl` only configures the protocol versions and cipher suites. |
| V12.1.5 | 3 | Verify that Encrypted Client Hello (ECH) is enabled in the application's TLS settings to prevent exposure of sensitive metadata, such as the Server Name Indication (SNI), during TLS handshake processes. | Verification required | The JDK's and Tomcat's current TLS stack does not support Encrypted Client Hello. Revisit once the platform supports it. |
| V12.2.1 | 1 | Verify that TLS is used for all connectivity between a client and external facing, HTTP-based services, and does not fall back to insecure or unencrypted communications. | Configured | The production profile enables TLS only, with no plain-HTTP connector or fallback; `local` and `test` disable TLS only for local HTTP tooling and must not be deployed. |
| V12.2.2 | 1 | Verify that external facing services use publicly trusted TLS certificates. | Deployment decision required | The server certificate is supplied through `CERTIFICATE_PEM`/`PRIVATE_KEY_PEM` environment variables; whether it is issued by a publicly trusted CA is a deployment decision the template cannot make. |
| V12.3.1 | 2 | Verify that an encrypted protocol such as TLS is used for all inbound and outbound connections to and from the application, including monitoring systems, management tools, remote access and SSH, middleware, databases, mainframes, partner systems, or external APIs. The server must not fall back to insecure or unencrypted protocols. | Deployment decision required | The application's own outbound call, to Keycloak's account endpoint, uses whatever scheme the configured issuer URI specifies. Database connections, monitoring, and remote access are deployment infrastructure the template does not configure. |
| V12.3.2 | 2 | Verify that TLS clients validate certificates received before communicating with a TLS server. | Verify framework behavior | The JDK's default HTTPS client validates the server certificate against the platform trust store; the application overrides no `TrustManager` or `SSLContext` to bypass this. |
| V12.3.3 | 2 | Verify that TLS or another appropriate transport encryption mechanism used for all connectivity between internal, HTTP-based services within the application, and does not fall back to insecure or unencrypted communications. | Deployment decision required | Whether the Keycloak instance this application calls is reachable only over TLS is determined by the deployment topology and the configured issuer URI; see V12.3.1. |
| V12.3.4 | 2 | Verify that TLS connections between internal services use trusted certificates. Where internally generated or self-signed certificates are used, the consuming service must be configured to only trust specific internal CAs and specific self-signed certificates. | Configured | `server.ssl.bundle.pem.server.truststore.certificate` (`CA_BUNDLE_PEM`) lets a deployment supply a specific internal CA bundle rather than trusting the platform default; populating it with the correct internal CA for a given deployment is that deployment's decision. |
| V12.3.5 | 3 | Verify that services communicating internally within a system (intra-service communications) use strong authentication to ensure that each endpoint is verified. Strong authentication methods, such as TLS client authentication, must be employed to ensure identity, using public-key infrastructure and mechanisms that are resistant to replay attacks. For microservice architectures, consider using a service mesh to simplify certificate management and enhance security. | Verification required | The application's one inter-service call, to Keycloak's account endpoint, is authenticated with the calling user's own OAuth2 bearer token (see V8.3.3), not mTLS or a service mesh. Whether that satisfies a given deployment's threat model for "strong" endpoint authentication is a decision the template does not make. |

## V13: Configuration

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V13.1.1 | 2 | Verify that all communication needs for the application are documented. This must include external services which the application relies upon and cases where an end user might be able to provide an external location to which the application will then connect. | Configured | The application's one external dependency, Keycloak, is documented in [Authentication](authentication.md) and [ADR 0004](../adr/0004-keycloak-authentication-local-authorisation.md). No feature lets a user supply an arbitrary external destination for the application to connect to. |
| V13.1.2 | 3 | Verify that for each service the application uses, the documentation defines the maximum number of concurrent connections (e.g., connection pool limits) and how the application behaves when that limit is reached, including any fallback or recovery mechanisms, to prevent denial of service conditions. | Deployment decision required | Connection-pool limits for the database and behavior when they are exhausted are Spring Boot/HikariCP defaults, not documented or tuned by the template; this is an operational decision for the deployment. |
| V13.1.3 | 3 | Verify that the application documentation defines resource‑management strategies for every external system or service it uses (e.g., databases, file handles, threads, HTTP connections). This should include resource‑release procedures, timeout settings, failure handling, and where retry logic is implemented, specifying retry limits, delays, and back‑off algorithms. For synchronous HTTP request–response operations it should mandate short timeouts and either disable retries or strictly limit retries to prevent cascading delays and resource exhaustion. | Deployment decision required | `RestClientConfiguration` sets an explicit connect and read timeout for the one outbound call the application makes (see V16.5.2), but no retry policy is defined for it, and database connection-pool resource management is left to Spring Boot/HikariCP defaults. Document and tune these for the deployment. |
| V13.1.4 | 3 | Verify that the application's documentation defines the secrets that are critical for the security of the application and a schedule for rotating them, based on the organization's threat model and business requirements. | Deployment decision required | [ADR 0006](../adr/0006-tls-and-oauth-client-key-management.md) and [Authentication](authentication.md) identify the JWKS signing/encryption key pair as a critical secret requiring rotation in coordination with Keycloak, but no rotation schedule is defined. TLS certificate and database credential rotation schedules are entirely deployment decisions. |
| V13.2.1 | 2 | Verify that communications between backend application components that don't support the application's standard user session mechanism, including APIs, middleware, and data layers, are authenticated. Authentication must use individual service accounts, short-term tokens, or certificate-based authentication and not unchanging credentials such as passwords, API keys, or shared accounts with privileged access. | Deployment decision required | How the application authenticates to the database, and whether that credential is short-term or certificate-based rather than a static password, is a secret-manager and deployment decision the template does not make. |
| V13.2.2 | 2 | Verify that communications between backend application components, including local or operating system services, APIs, middleware, and data layers, are performed with accounts assigned the least necessary privileges. | Deployment decision required | [Sessions](sessions.md#design-and-ownership) states the principle that the runtime database account should have only the data-access permissions the service needs, separate from the CI migration account's DDL privileges, but provisioning the actual accounts with those privileges is a deployment action. |
| V13.2.3 | 2 | Verify that if a credential has to be used for service authentication, the credential being used by the consumer is not a default credential (e.g., root/root or admin/admin). | Deployment decision required | Whether the database or any other backend credential is a default or shared credential is determined entirely by how the deployment provisions it. |
| V13.2.4 | 2 | Verify that an allowlist is used to define the external resources or systems with which the application is permitted to communicate (e.g., for outbound requests, data loads, or file access). This allowlist can be implemented at the application layer, web server, firewall, or a combination of different layers. | Deployment decision required | An allowlist of external systems the application may contact is a network or application-layer control configured by the deployment; the template's only outbound call is to the configured Keycloak issuer. |
| V13.2.5 | 2 | Verify that the web or application server is configured with an allowlist of resources or systems to which the server can send requests or load data or files from. | Deployment decision required | Restricting which resources the web or application server can request is a server or network-layer control configured by the deployment. |
| V13.2.6 | 3 | Verify that where the application connects to separate services, it follows the documented configuration for each connection, such as maximum parallel connections, behavior when maximum allowed connections is reached, connection timeouts, and retry strategies. | Deployment decision required | `RestClientConfiguration` defines a connect and read timeout for its one outbound connection (see V16.5.2); maximum parallel connections and retry strategy for it, and for the database connection pool, remain undocumented deployment decisions. |
| V13.3.1 | 2 | Verify that a secrets management solution, such as a key vault, is used to securely create, store, control access to, and destroy backend secrets. These could include passwords, key material, integrations with databases and third-party systems, keys and seeds for time-based tokens, other internal secrets, and API keys. Secrets must not be included in application source code or included in build artifacts. For an L3 application, this must involve a hardware-backed solution such as an HSM. | Deployment decision required | Whether a secrets-management solution such as a key vault is used to store the JWKS key material, TLS private key, and database credentials is a deployment decision; the template only requires that they be supplied through configuration, not embedded in source (see V11.1.1). |
| V13.3.2 | 2 | Verify that access to secret assets adheres to the principle of least privilege. | Deployment decision required | Least-privilege access to whatever secrets store is used is a deployment and secret-manager decision. |
| V13.3.3 | 3 | Verify that all cryptographic operations are performed using an isolated security module (such as a vault or hardware security module) to securely manage and protect key material from exposure outside of the security module. | Deployment decision required | Use of an isolated security module such as an HSM or vault for cryptographic operations is a deployment decision; the template performs its cryptographic operations (TLS, JWT signing) in the JVM process. |
| V13.3.4 | 3 | Verify that secrets are configured to expire and be rotated based on the application's documentation. | Deployment decision required | Secret expiration and rotation scheduling is a deployment decision; see the related gap at V13.1.4 for the one secret the template documents handling rules for. |
| V13.4.1 | 1 | Verify that the application is deployed either without any source control metadata, including the .git or .svn folders, or in a way that these folders are inaccessible both externally and to the application itself. | Deployment decision required | Whether the deployed artifact or container image includes source-control metadata is determined by the build and packaging process, not application code. |
| V13.4.2 | 2 | Verify that debug modes are disabled for all components in production environments to prevent exposure of debugging features and information leakage. | Configured | The application defines no debug-mode toggle; its generic RFC 9457 Problem Details error responses (see V16.5.1) apply in every profile, including `local` and `test`. Do not introduce a debug flag that changes this. |
| V13.4.3 | 2 | Verify that web servers do not expose directory listings to clients unless explicitly intended. | Configured | The application serves no static resource directory at all; there is nothing for a directory listing to expose. |
| V13.4.4 | 2 | Verify that using the HTTP TRACE method is not supported in production environments, to avoid potential information leakage. | Configured | `TomcatConfiguration` explicitly disables HTTP TRACE and has a regression test. |
| V13.4.5 | 2 | Verify that documentation (such as for internal APIs) and monitoring endpoints are not exposed unless explicitly intended. | Configured | The application includes no API-documentation generator (such as springdoc/Swagger) and no monitoring endpoints; `spring-boot-starter-actuator` is not a dependency. An adopter who adds either must explicitly control its exposure. |
| V13.4.6 | 3 | Verify that the application does not expose detailed version information of backend components. | Configured | `TomcatConfiguration` disables `X-Powered-By` and removes application-provided `Server` header values (`serverRemoveAppProvidedValues`); see `headers.md` ("Server", "X-Powered-By"). |
| V13.4.7 | 3 | Verify that the web tier is configured to only serve files with specific file extensions to prevent unintentional information, configuration, and source code leakage. | Configured | The application serves no static files at all, so there is no file-extension-based restriction to configure. Reassess if static content is ever added. |

## V14: Data Protection

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V14.1.1 | 2 | Verify that all sensitive data created and processed by the application has been identified and classified into protection levels. This includes data that is only encoded and therefore easily decoded, such as Base64 strings or the plaintext payload inside a JWT. Protection levels need to take into account any data protection and privacy regulations and standards which the application is required to comply with. | Verification required | No sensitive-data classification exists for this base template; the fields it does manage (username, display name, email in the local user directory; OIDC claims held in the session) are not yet classified into protection levels. An adopter must classify the data their domain model introduces. |
| V14.1.2 | 2 | Verify that all sensitive data protection levels have a documented set of protection requirements. This must include (but not be limited to) requirements related to general encryption, integrity verification, retention, how the data is to be logged, access controls around sensitive data in logs, database-level encryption, privacy and privacy-enhancing technologies to be used, and other confidentiality requirements. | Verification required | Protection requirements depend on the classification at V14.1.1, which does not yet exist. |
| V14.2.1 | 1 | Verify that sensitive data is only sent to the server in the HTTP message body or header fields, and that the URL and query string do not contain sensitive information, such as an API key or session token. | Verify framework behavior | The OAuth2/OIDC authorization-code flow carries `code`, `state`, and related parameters in the redirect query string, as the protocol requires; the application adds no further sensitive data to any URL or query string of its own, and `RequestLoggingFilter`'s `QUERY_PARAMETER_REDACT_LIST` redacts these OAuth/OIDC values from its own logs. |
| V14.2.2 | 2 | Verify that the application prevents sensitive data from being cached in server components, such as load balancers and application caches, or ensures that the data is securely purged after use. | Configured | Spring Security's default `Cache-Control: no-cache, no-store, max-age=0, must-revalidate` header is applied to authenticated responses; see `headers.md` ("Cache-Control"). |
| V14.2.3 | 2 | Verify that defined sensitive data is not sent to untrusted parties (e.g., user trackers) to prevent unwanted collection of data outside of the application's control. | Not applicable to this template | The application integrates no third-party tracker, analytics, or similar service that data could be sent to. |
| V14.2.4 | 2 | Verify that controls around sensitive data related to encryption, integrity verification, retention, how the data is to be logged, access controls around sensitive data in logs, privacy and privacy-enhancing technologies, are implemented as defined in the documentation for the specific data's protection level. | Verification required | Controls cannot yet be verified against a documented protection level, since none is defined; see V14.1.1. |
| V14.2.5 | 3 | Verify that caching mechanisms are configured to only cache responses which have the expected content type for that resource and do not contain sensitive, dynamic content. The web server should return a 404 or 302 response when a non-existent file is accessed rather than returning a different, valid file. This should prevent Web Cache Deception attacks. | Configured | `ApiResponseEntityExceptionHandler.handleNoResourceFoundException` returns a generic 404 Problem Details response for any unmatched route; the application never falls through to serving different, valid content for a non-existent path. |
| V14.2.6 | 3 | Verify that the application only returns the minimum required sensitive data for the application's functionality. For example, only returning some of the digits of a credit card number and not the full number. If the complete data is required, it should be masked in the user interface unless the user specifically views it. | Verification required | The administration API returns full user, group, and role records to callers who already hold the corresponding management authority; no field-level masking or partial-data policy is defined. This is a product decision for whichever domain model an adopter introduces, not something the admin API itself needs today. |
| V14.2.7 | 3 | Verify that sensitive information is subject to data retention classification, ensuring that outdated or unnecessary data is deleted automatically, on a defined schedule, or as the situation requires. | Verification required | No automated data-retention or deletion schedule is implemented; deleting a user, group, or role happens only through an explicit administration API call. |
| V14.2.8 | 3 | Verify that sensitive information is removed from the metadata of user-submitted files unless storage is consented to by the user. | Not applicable to this template | The application accepts no user-submitted files; there is no file metadata to strip. See V5. |
| V14.3.1 | 1 | Verify that authenticated data is cleared from client storage, such as the browser DOM, after the client or session is terminated. The 'Clear-Site-Data' HTTP response header field may be able to help with this but the client-side should also be able to clear up if the server connection is not available when the session is terminated. | Not applicable to this template | The application has no client-side storage or DOM state to clear; it has no bundled frontend. |
| V14.3.2 | 2 | Verify that the application sets sufficient anti-caching HTTP response header fields (i.e., Cache-Control: no-store) so that sensitive data is not cached in browsers. | Configured | Spring Security's default `Cache-Control: no-cache, no-store, max-age=0, must-revalidate` header is applied to authenticated responses; see `headers.md` ("Cache-Control"). This is the same control recorded at V14.2.2. |
| V14.3.3 | 2 | Verify that data stored in browser storage (such as localStorage, sessionStorage, IndexedDB, or cookies) does not contain sensitive data, with the exception of session tokens. | Not applicable to this template | The application has no bundled frontend and stores nothing in browser storage of any kind. |

## V15: Secure Coding and Architecture

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V15.1.1 | 1 | Verify that application documentation defines risk based remediation time frames for 3rd party component versions with vulnerabilities and for updating libraries in general, to minimize the risk from these components. | Deployment decision required | No risk-based remediation timeframe for vulnerable or outdated dependencies is documented; this is an organizational patching policy decision, not something the template's code can decide. |
| V15.1.2 | 2 | Verify that an inventory catalog, such as software bill of materials (SBOM), is maintained of all third-party libraries in use, including verifying that components come from pre-defined, trusted, and continually maintained repositories. | Verification required | No formal software bill of materials is generated; `pom.xml` declares every direct dependency with a version managed by the Spring Boot BOM, and Maven resolves them from the default, trusted Maven Central repository. An adopter should add SBOM generation (for example `cyclonedx-maven-plugin`) for a formal inventory. |
| V15.1.3 | 2 | Verify that the application documentation identifies functionality which is time-consuming or resource-demanding. This must include how to prevent a loss of availability due to overusing this functionality and how to avoid a situation where building a response takes longer than the consumer's timeout. Potential defenses may include asynchronous processing, using queues, and limiting parallel processes per user and per application. | Configured | The one resource-intensive operation the base template has, listing users, groups, and roles, is bounded: `AdminPageable` caps page size at 100 via `@Max(100)` on every list endpoint. |
| V15.1.4 | 3 | Verify that application documentation highlights third-party libraries which are considered to be "risky components". | Verification required | No third-party library is documented as a risky component. This is an ongoing dependency-review practice for the adopting team, not a one-time template decision. |
| V15.1.5 | 3 | Verify that application documentation highlights parts of the application where "dangerous functionality" is being used. | Verification required | No part of the codebase is labeled as containing dangerous functionality; the closest existing equivalent is this crosswalk and the per-topic security docs (`authentication.md`, `authorization.md`, `sessions.md`) identifying sensitive areas such as JWKS key handling and privilege resolution. |
| V15.2.1 | 1 | Verify that the application only contains components which have not breached the documented update and remediation time frames. | Deployment decision required | Whether any currently used component has breached a remediation deadline depends on the deadline defined at V15.1.1, which does not exist yet, and on an ongoing dependency-scanning practice the template does not run. |
| V15.2.2 | 2 | Verify that the application has implemented defenses against loss of availability due to functionality which is time-consuming or resource-demanding, based on the documented security decisions and strategies for this. | Configured | The same page-size cap recorded at V15.1.3 (`AdminPageable`, `@Max(100)`) is the implemented defense for the one resource-intensive operation in the base template. |
| V15.2.3 | 2 | Verify that the production environment only includes functionality that is required for the application to function, and does not expose extraneous functionality such as test code, sample snippets, and development functionality. | Configured | Test code lives under `src/test/java` and is never packaged into the application artifact, per Maven's standard build layout. The `local` and `test` profiles' TLS and cookie relaxations are documented, intentional development-only overrides (`application-local.yaml`, `application-test.yaml`) that must not be deployed. |
| V15.2.4 | 3 | Verify that third-party components and all of their transitive dependencies are included from the expected repository, whether internally owned or an external source, and that there is no risk of a dependency confusion attack. | Configured | `pom.xml` declares no custom Maven repository; dependencies resolve only from the default, well-known Maven Central, so there is no shadowable internal package name to be confused with a public one. |
| V15.2.5 | 3 | Verify that the application implements additional protections around parts of the application which are documented as containing "dangerous functionality" or using third-party libraries considered to be "risky components". This could include techniques such as sandboxing, encapsulation, containerization or network level isolation to delay and deter attackers who compromise one part of an application from pivoting elsewhere in the application. | Verification required | No part of the application is labeled as dangerous functionality or a risky component (see V15.1.4, V15.1.5), so no additional isolation has been applied around one. |
| V15.3.1 | 1 | Verify that the application only returns the required subset of fields from a data object. For example, it should not return an entire data object, as some individual fields should not be accessible to users. | Configured | The admin API never returns a JPA entity directly; `UserResponse`, `GroupResponse`, and `RoleResponse` are explicit response records that name only the fields intended to be exposed. |
| V15.3.2 | 2 | Verify that where the application backend makes calls to external URLs, it is configured to not follow redirects unless it is intended functionality. | Verification required | The application does not explicitly configure redirect-following behavior for its one outbound call (`AccountController` to Keycloak); it relies on the JDK HTTP client's default of following redirects. The call's target is fixed and trusted, so a malicious redirect is not attacker-reachable, but the behavior itself is a platform default, not an explicit decision. |
| V15.3.3 | 2 | Verify that the application has countermeasures to protect against mass assignment attacks by limiting allowed fields per controller and action, e.g., it is not possible to insert or update a field value when it was not intended to be part of that action. | Configured | The admin API only ever binds a request body to an explicit request record (`UserCreateRequest`, `UserUpdateRequest`, and the equivalents for groups and roles), never to a JPA entity, so a request cannot set a field the DTO does not declare. |
| V15.3.4 | 2 | Verify that all proxying and middleware components transfer the user's original IP address correctly using trusted data fields that cannot be manipulated by the end user, and the application and web server use this correct value for logging and security decisions such as rate limiting, taking into account that even the original IP address may not be reliable due to dynamic IPs, VPNs, or corporate firewalls. | Configured | `ClientIpResolver.none()` is the default bean, so no header is trusted by default; when a deployment wires up `TrustedHeaderClientIpResolver`, `XForwardedForClientIpResolver`, or `CloudFrontViewerAddressClientIpResolver`, each only honors the forwarded-address header when the immediate peer matches a configured trusted-proxy CIDR, and `RequestLoggingFilter`/`SecurityLoggingContextFilter` use the resulting value for logging. This is the same mechanism recorded at V4.1.3. |
| V15.3.5 | 2 | Verify that the application explicitly ensures that variables are of the correct type and performs strict equality and comparator operations. This is to avoid type juggling or type confusion vulnerabilities caused by the application code making an assumption about a variable type. | Verify framework behavior | Java is statically typed with no loose-equality operator; the application performs no dynamic type coercion of the kind that causes type-juggling vulnerabilities in dynamically typed languages. |
| V15.3.6 | 2 | Verify that JavaScript code is written in a way that prevents prototype pollution, for example, by using Set() or Map() instead of object literals. | Not applicable to this template | The application has no client-side JavaScript. |
| V15.3.7 | 2 | Verify that the application has defenses against HTTP parameter pollution attacks, particularly if the application framework makes no distinction about the source of request parameters (query string, body parameters, cookies, or header fields). | Verify framework behavior | Every request parameter is bound through Spring MVC's `@RequestParam`/`@PathVariable` machinery, which has consistent, documented behavior for a given parameter type; the application does no manual merging of query, body, cookie, and header values of the same name. |
| V15.4.1 | 3 | Verify that shared objects in multi-threaded code (such as caches, files, or in-memory objects accessed by multiple threads) are accessed safely by using thread-safe types and synchronization mechanisms like locks or semaphores to avoid race conditions and data corruption. | Configured | The one piece of shared mutable state across requests, `WebSecurityConfiguration`'s `jwtDecoders` cache, is a `ConcurrentHashMap` populated with `computeIfAbsent`, which is safe for concurrent access. Spring's other singleton beans (controllers, services, repositories) hold no other mutable shared state. |
| V15.4.2 | 3 | Verify that checks on a resource's state, such as its existence or permissions, and the actions that depend on them are performed as a single atomic operation to prevent time-of-check to time-of-use (TOCTOU) race conditions. For example, checking if a file exists before opening it, or verifying a user’s access before granting it. | Configured | `AdministrationService`'s create methods check uniqueness (`existsByUsername`/`existsByName`) before saving, which is not itself atomic, but a database-level `UNIQUE` constraint on `username`, group `name`, and role `name` (`001-authorisation-schema.sql`) is the authoritative backstop: a race can produce, at worst, an unhandled constraint-violation error, never an actual duplicate or inconsistent record. |
| V15.4.3 | 3 | Verify that locks are used consistently to avoid threads getting stuck, whether by waiting on each other or retrying endlessly, and that locking logic stays within the code responsible for managing the resource to ensure locks cannot be inadvertently or maliciously modified by external classes or code. | Not applicable to this template | The application uses no explicit lock, semaphore, or other manual synchronization anywhere in its own code; the concurrency safety it needs comes from the database's transaction isolation and unique constraints (see V15.4.2). |
| V15.4.4 | 3 | Verify that resource allocation policies prevent thread starvation by ensuring fair access to resources, such as by leveraging thread pools, allowing lower-priority threads to proceed within a reasonable timeframe. | Verify framework behavior | Request handling uses Tomcat's own bounded connector thread pool, and `AsyncConfiguration` builds its task executor through Spring Boot's `ThreadPoolTaskExecutorBuilder`; the application defines no unbounded or ad hoc thread creation that could starve other threads. |

## V16: Security Logging and Error Handling

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V16.1.1 | 2 | Verify that an inventory exists documenting the logging performed at each layer of the application's technology stack, what events are being logged, log formats, where that logging is stored, how it is used, how access to it is controlled, and for how long logs are kept. | Configured | [Security logging](logging/README.md), [schema](logging/schema.md), and [event reference](logging/event-reference.md) together are the inventory: what is logged, its format, and where responsibility shifts to the deployment (storage, access control, retention) are documented in `logging/README.md`'s recommendation matrix and "Required deployment decisions". |
| V16.2.1 | 2 | Verify that each log entry includes necessary metadata (such as when, where, who, what) that would allow for a detailed investigation of the timeline when an event happens. | Configured | Every event carries a timestamp, service/environment metadata, request ID, `event.category`/`type`/`action`/`outcome`, method, path, and user where known; see `logging/README.md` ("Record the 'when, where, who, and what' of an event") and `schema.md`. |
| V16.2.2 | 2 | Verify that time sources for all logging components are synchronized, and that timestamps in security event metadata use UTC or include an explicit time zone offset. UTC is recommended to ensure consistency across distributed systems and to prevent confusion during daylight saving time transitions. | Configured | ECS emits `@timestamp` in UTC; see `logging/README.md` ("Synchronize clocks and record timestamps consistently"). Synchronizing clocks across hosts or containers (NTP or the cloud platform's time service) is a deployment responsibility the template cannot configure. |
| V16.2.3 | 2 | Verify that the application only stores or broadcasts logs to the files and services that are documented in the log inventory. | Configured | The application writes exclusively to stdout; it stores or broadcasts logs nowhere else. See `logging/README.md` ("Use a centralized log collection system and record to stdout where appropriate"). Where that stream is shipped downstream is a deployment decision, documented as such. |
| V16.2.4 | 2 | Verify that logs can be read and correlated by the log processor that is in use, preferably by using a common logging format. | Configured | Spring Boot's ECS JSON formatter produces a standard, documented format; see `schema.md` for shared fields and `event-reference.md` for each event's contract. |
| V16.2.5 | 2 | Verify that when logging sensitive data, the application enforces logging based on the data's protection level. For example, it may not be allowed to log certain data, such as credentials or payment details. Other data, such as session tokens, may only be logged by being hashed or masked, either in full or partially. | Configured | Bodies, cookies, authorization headers, passwords, tokens, and session IDs are never logged; query values are redacted by name via `WebSecurityConfiguration.QUERY_PARAMETER_REDACT_LIST`. See `logging/README.md` ("Exclude or mask secrets, credentials, tokens, keys, session identifiers..."). Review new endpoints for secrets in query values or path segments before release. |
| V16.3.1 | 2 | Verify that all authentication operations are logged, including successful and unsuccessful attempts. Additional metadata, such as the type of authentication or factors used, should also be collected. | Configured | `SecurityAuditEventLogger` records login success and failure with outcome, user name, and failure exception type, without credentials or tokens. Authentication-factor metadata does not apply; factor selection and evaluation are entirely Keycloak's responsibility (see [Authentication](authentication.md)). |
| V16.3.2 | 2 | Verify that failed authorization attempts are logged. For L3, this must include logging all authorization decisions, including logging when sensitive data is accessed (without logging the sensitive data itself). | Configured | Authorization denials are logged with category, type, action, and outcome (`SecurityAuditEventLogger`, `ProblemDetailAccessDeniedHandler` for CSRF). The L3 extension of logging every authorization decision, including sensitive-data access, is not implemented; an adopter targeting L3 must add it at the point sensitive data is read. |
| V16.3.3 | 2 | Verify that the application logs the security events that are defined in the documentation and also logs attempts to bypass the security controls, such as input validation, business logic, and anti-automation. | Configured | `ApiResponseEntityExceptionHandler` logs a `validate_input` event for every rejected Bean Validation field or constraint, covering input-validation bypass attempts. The template has no business-logic rules or anti-automation controls to bypass; an adopter who adds either must add corresponding bypass-attempt logging. |
| V16.3.4 | 2 | Verify that the application logs unexpected errors and security control failures such as backend TLS failures. | Configured | `ApiResponseEntityExceptionHandler.handleUnexpectedException` logs every unexpected exception at `ERROR` with a safe error type, request path, and correlation ID. The row's own example, backend TLS failures, is out of the application's log surface; `logging/README.md` documents that TLS handshake, network, and WAF failures belong to the load balancer, proxy, or servlet container. |
| V16.4.1 | 2 | Verify that all logging components appropriately encode data to prevent log injection. | Configured | Values are passed as structured key-values to the ECS JSON formatter rather than concatenated into message strings, so log-record structure cannot be altered by request-derived data; see `logging/README.md` ("Sanitize event data to prevent log injection"). |
| V16.4.2 | 2 | Verify that logs are protected from unauthorized access and cannot be modified. | Deployment decision required | Console/collector access control, log storage permissions, and tamper protection are entirely deployment/platform concerns; see `logging/README.md` ("Restrict access to logs": Deployment responsibility). |
| V16.4.3 | 2 | Verify that logs are securely transmitted to a logically separate system for analysis, detection, alerting, and escalation. The aim is to ensure that if the application is breached, the logs are not compromised. | Deployment decision required | The application only writes to stdout; secure transmission to a separate analysis system is the collector/SIEM's responsibility. See `logging/README.md` ("Protect log collection, transit, storage, and backups...": Deployment responsibility) and "Required deployment decisions" item 1. |
| V16.5.1 | 2 | Verify that a generic message is returned to the consumer when an unexpected or security-sensitive error occurs, ensuring no exposure of sensitive internal system data such as stack traces, queries, secret keys, and tokens. | Configured | Tomcat-level errors (`TomcatProblemDetailErrorReportValve`) and MVC errors (`ApiResponseEntityExceptionHandler.handleUnexpectedException`) return generic RFC 9457 Problem Details with no stack trace, query, or secret; see [Security and API error responses](error-responses.md), `logging/README.md`, and `hardening.md` control 2.5. |
| V16.5.2 | 2 | Verify that the application continues to operate securely when external resource access fails, for example, by using patterns such as circuit breakers or graceful degradation. | Verification required | `RestClientConfiguration` configures a 5-second connect timeout and a 10-second read timeout, so a slow or unresponsive Keycloak no longer hangs the `/account` call indefinitely. `ApiResponseEntityExceptionHandler.handleRestClientResponseException` still converts any resulting upstream failure into a generic Problem Details response without leaking its detail. There is no retry limit or circuit breaker for outbound calls or the database connection pool; an adopter must decide on and implement those resilience patterns for their own external dependencies. |
| V16.5.3 | 2 | Verify that the application fails gracefully and securely, including when an exception occurs, preventing fail-open conditions such as processing a transaction despite errors resulting from validation logic. | Configured | `@Valid` request validation runs before any service or repository call, `@Transactional` on `AdministrationService` rolls back a failed operation rather than partially applying it, and every exception handled by `ApiResponseEntityExceptionHandler` returns an error response rather than falling through to a success path. |
| V16.5.4 | 3 | Verify that a "last resort" error handler is defined which will catch all unhandled exceptions. This is both to avoid losing error details that must go to log files and to ensure that an error does not take down the entire application process, leading to a loss of availability. | Configured | `ApiResponseEntityExceptionHandler`'s `@ExceptionHandler(Exception.class)` method is the last-resort handler: it catches any exception not handled elsewhere, logs it, and returns a generic 500 Problem Details response instead of letting the servlet container fail the process. |

## V17: WebRTC

| ASVS ID | L | Requirement | Status | Template and deployment comment |
| --- | --- | --- | --- | --- |
| V17.1.1 | 2 | Verify that the Traversal Using Relays around NAT (TURN) service only allows access to IP addresses that are not reserved for special purposes (e.g., internal networks, broadcast, loopback). Note that this applies to both IPv4 and IPv6 addresses. | Not applicable to this template | The template does not implement WebRTC. Reassess this chapter before adding WebRTC capabilities. |
| V17.1.2 | 3 | Verify that the Traversal Using Relays around NAT (TURN) service is not susceptible to resource exhaustion when legitimate users attempt to open a large number of ports on the TURN server. | Not applicable to this template | The template does not implement WebRTC. Reassess this chapter before adding WebRTC capabilities. |
| V17.2.1 | 2 | Verify that the key for the Datagram Transport Layer Security (DTLS) certificate is managed and protected based on the documented policy for management of cryptographic keys. | Not applicable to this template | The template does not implement WebRTC. Reassess this chapter before adding WebRTC capabilities. |
| V17.2.2 | 2 | Verify that the media server is configured to use and support approved Datagram Transport Layer Security (DTLS) cipher suites and a secure protection profile for the DTLS Extension for establishing keys for the Secure Real-time Transport Protocol (DTLS-SRTP). | Not applicable to this template | The template does not implement WebRTC. Reassess this chapter before adding WebRTC capabilities. |
| V17.2.3 | 2 | Verify that Secure Real-time Transport Protocol (SRTP) authentication is checked at the media server to prevent Real-time Transport Protocol (RTP) injection attacks from leading to either a Denial of Service condition or audio or video media insertion into media streams. | Not applicable to this template | The template does not implement WebRTC. Reassess this chapter before adding WebRTC capabilities. |
| V17.2.4 | 2 | Verify that the media server is able to continue processing incoming media traffic when encountering malformed Secure Real-time Transport Protocol (SRTP) packets. | Not applicable to this template | The template does not implement WebRTC. Reassess this chapter before adding WebRTC capabilities. |
| V17.2.5 | 3 | Verify that the media server is able to continue processing incoming media traffic during a flood of Secure Real-time Transport Protocol (SRTP) packets from legitimate users. | Not applicable to this template | The template does not implement WebRTC. Reassess this chapter before adding WebRTC capabilities. |
| V17.2.6 | 3 | Verify that the media server is not susceptible to the "ClientHello" Race Condition vulnerability in Datagram Transport Layer Security (DTLS) by checking if the media server is publicly known to be vulnerable or by performing the race condition test. | Not applicable to this template | The template does not implement WebRTC. Reassess this chapter before adding WebRTC capabilities. |
| V17.2.7 | 3 | Verify that any audio or video recording mechanisms associated with the media server are able to continue processing incoming media traffic during a flood of Secure Real-time Transport Protocol (SRTP) packets from legitimate users. | Not applicable to this template | The template does not implement WebRTC. Reassess this chapter before adding WebRTC capabilities. |
| V17.2.8 | 3 | Verify that the Datagram Transport Layer Security (DTLS) certificate is checked against the Session Description Protocol (SDP) fingerprint attribute, terminating the media stream if the check fails, to ensure the authenticity of the media stream. | Not applicable to this template | The template does not implement WebRTC. Reassess this chapter before adding WebRTC capabilities. |
| V17.3.1 | 2 | Verify that the signaling server is able to continue processing legitimate incoming signaling messages during a flood attack. This should be achieved by implementing rate limiting at the signaling level. | Not applicable to this template | The template does not implement WebRTC. Reassess this chapter before adding WebRTC capabilities. |
| V17.3.2 | 2 | Verify that the signaling server is able to continue processing legitimate signaling messages when encountering malformed signaling message that could cause a denial of service condition. This could include implementing input validation, safely handling integer overflows, preventing buffer overflows, and employing other robust error-handling techniques. | Not applicable to this template | The template does not implement WebRTC. Reassess this chapter before adding WebRTC capabilities. |
